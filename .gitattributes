# Batch files must keep CRLF line endings. cmd.exe mis-parses a .cmd saved with
# Unix endings, so refresh-results.cmd stopped working when an edit rewrote it
# with LF (2026-09-23). This pins it whatever the editor or the platform does.
*.cmd text eol=crlf
*.bat text eol=crlf
