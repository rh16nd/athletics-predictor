# PodiumCall (2026 Diamond League Predictor) — Handoff

_Last updated: 2026-08-23 (fourth session, same day) — both repos committed and pushed_

## Goal

A fully automated ML prediction system for the 2026 Diamond League Final (Brussels, Sep 4-5), branded **PodiumCall**. It scrapes live World Athletics data, checks for injuries/withdrawals, runs a trained model, and serves win-probability predictions to a dashboard. Two repos:

```
C:\Users\rayen\athletics-predictor\   ← Python ML pipeline
C:\Users\rayen\track-insights-main\  ← React dashboard (TanStack Router + Vite), "PodiumCall" front end
```

**Priority order, per the user**: model accuracy and data honesty first; visual/UX polish was deliberately saved for last, then explicitly picked back up 2026-08-23 (see track-insights-main's Current State below — landing page, branding, and a design critique pass are now done). When in doubt about what to work on next, prefer real data over hardcoded/hand-typed data, and always isolate a change's effect before trusting a number that moved.

## How to Run

```powershell
# Terminal 1 — Flask API
cd C:\Users\rayen\athletics-predictor
venv\Scripts\activate
python api.py   # http://localhost:5000

# Terminal 2 — React dashboard
cd C:\Users\rayen\track-insights-main
npm run dev     # http://localhost:8080 (or 8081 if 8080 is taken)

# Refresh live predictions (scrapes World Athletics, ~1hr — covers all 32 disciplines + injury check)
cd C:\Users\rayen\athletics-predictor
venv\Scripts\activate
python run.py

# Rebuild real DL Final ground-truth labels (~1 min, hits a public WA API)
python src/dl_final_results_scraper.py

# Enrich training data with real per-meeting season history (~few min)
python src/season_results_scraper.py

# Add real results from Olympics/Worlds/Continental Tour Gold/Euro Champs (~few min)
python src/major_meets_scraper.py

# Retrain the model (fast, seconds — uses already-scraped data/raw/*.csv)
python src/train_model.py --with-recency --with-h2h

# Grid-search hyperparameters via walk-forward folds (informational, prints only)
python src/train_model.py --with-recency --with-h2h --tune

# Run the test suite
python -m pytest
```

Both dev servers auto-reload on code changes (Flask debug mode, Vite HMR) and re-read data files fresh on every request — no restart needed after `run.py` or a retrain.

## Current State

**Model**: RandomForestClassifier, `n_estimators=200, max_depth=16, min_samples_leaf=1, class_weight=None` (`DEFAULT_MODEL_PARAMS` in `train_model.py` — walk-forward tuned via `--tune`, re-tune whenever the feature set, training-set size, OR data-quality filtering changes, since the winning config shifts each time — it's flipped `class_weight` None↔"balanced" twice across five re-tunes so far; hyperparameter search on a dataset this size (~459 rows) is itself somewhat noisy, so don't read too much into which single config "won" a given round). 14 features, **all carrying real signal**:
`season_best, career_best, pb_gap, meets_count, consistency, yoy_improvement, age, season_rank, season_percentile, weighted_season_best, wind_adj_season_best, recent_trend, days_since_last, h2h_win_rate`

**Backtest accuracy: 60.1%** (276/459) — walk-forward validated: trained on every year strictly before each test year, scored independently on 2021, 2022, 2023, 2024, and 2025 (5 folds, not one fixed holdout). The deployed model is refit on all 7 label years after validation. Trained on all 32 live-predicted disciplines (`men_5000m` has thin signal — see Known Limitations). This is essentially flat vs. the same-day earlier number (60.3%) despite two real additions since (major meets beyond the DL circuit, and a noise filter) — see the Failed Attempts / project memory for why that's an expected outcome of this dataset's size, not a wasted afternoon.

**Ground truth is real, not hand-typed.** `src/dl_final_results_scraper.py` pulls actual Diamond League Final results (2018-2025, excluding 2020) directly from World Athletics' own public GraphQL API — the same API the site's own frontend uses (`x-api-key` is a public key shipped in every page load, not a secret). It finds each year's Final by filtering `rankingCategory == "DF"` and reads which disciplines were contested (and under what name — e.g. "Mile" some years instead of "1500 Metres") directly from what's present in the response, rather than a hand-maintained list. **2018/2019 were initially assumed to need a different scoring format** (the Final was split across two meetings, Zurich + Brussels) and skipped — checking the actual per-meeting data (2026-08-23) showed each of the 32 disciplines' DF group appears at exactly one of the two meetings, never both, so it's a two-city Final, not a split score. The scraper now aggregates across however many DF meetings a year has instead of assuming exactly one. 2020 (COVID-era "Inspiration Games" exhibition) is still deliberately excluded.

**Training features go beyond a season-best toplist.** `src/season_results_scraper.py` pulls every *regular-season* Diamond League meeting's results (not just the Final — that would leak the label) for 2018-2025 (excluding 2020), giving real multiple-marks-per-athlete-per-season data. This is what makes `meets_count`/`consistency`/`recent_trend` real features instead of structural zeros.

**Beyond the DL circuit: `src/major_meets_scraper.py`** (added 2026-08-23, user-requested "meets with the big athletes") adds real per-meeting results from the Olympic Games, the senior outdoor World Athletics Championships, World Athletics Continental Tour **Gold tier only** (`rankingCategory == "A"`, filtered out of a much larger Silver/Bronze/Challenger pool), and the European Athletics Championships (the other five Area Championships — African/Asian/American/Oceania/NACAC — were deliberately left out per the user, quality varies too much by continent). These groups were found via `getCalendarEvents`'s own `options.competitionGroups`/`rankingCategories` introspection, not guessed.

**Both per-meeting scrapers filter to "recognized" athletes only** (`dl_final_results_scraper.load_recognized_names`): a row is kept only if that athlete appears somewhere in the discipline's own toplist (any year, not just that exact year — deliberately lenient about single-year toplist gaps). This exists because a big meeting can still have a weak field in a discipline that isn't its main draw (e.g. a field-filler making a Continental Tour Gold meeting's javelin final) — keeping those marks would add noise (thin, one-off meets_count/consistency signal) rather than real information about the athletes DL Final prediction cares about. Drop rates are meaningful (e.g. women_800m dropped 98 of 254 candidate major-meet rows) — this is doing real work, not a no-op filter.

**Injury/withdrawal detection is fully wired end-to-end.** `src/injury_checker.py` scrapes news + meet-results recaps for injury/DNF signals, estimates recovery time, and either flags ("watch") or drops ("remove") an athlete from predictions. Both outcomes are visible on the dashboard: a "Watch" badge (linking to the real evidence) on flagged athletes, and a "Removed from predictions" panel (shown only when non-empty) for dropped ones.

**Test suite**: `tests/` — pure-function unit tests for `train_model.py`, `api.py`, `dl_final_results_scraper.py` (`python -m pytest`, no network/Selenium/Flask/real-files needed), plus `test_fixtures_integration.py` covering `build_labeled_dataset()`/`train_and_backtest()`/`load_predictions()` end to end against small checked-in fixtures (`tests/fixtures/`) instead of real scraped data. 37 tests total. Includes regression tests for the specific bugs described below.

**track-insights-main has a new local commit not yet pushed** — `d8f958b` (the theme/layout overhaul described in the tenth-round entry below: dark-then-warm-then-medium-terracotta palette, dashboard hero, centered nav). Last **pushed** commit there is still `1bd80d1`. athletics-predictor is pushed and up to date at `8c169fec` (no Python changes this session, only this file).

**track-insights-main (dashboard/frontend), 2026-08-23, first round:** the project was branded **PodiumCall** (domain-checked, `podiumcall.com`/`.io` both appeared unregistered) and a real landing page was built at `/`, with the former dashboard moved to `/dashboard` (sidebar nav updated). The landing page deliberately uses a dark theme structurally inspired by personaai.live (dark bg, gradient CTA, glass-bordered cards, a scrolling live-confidence ticker) but recolored to the dashboard's existing terracotta/gold/track-surface identity, per the user's "keep the track & field feel" direction — see `PRODUCT.md` (new, in that repo) for full product context, and `src/routes/index.tsx`/`src/styles.css`'s `.landing`-scoped tokens for the implementation. Installed several third-party Claude Code skills (`impeccable`, `design-taste-frontend`, Emil Kowalski's animation/design-eng set) via `npx skills add`/`npx impeccable install` — these land in `.agents/`/`.claude/skills/`, gitignored like the rest of the project's agent tooling. Ran `impeccable`'s dual-agent design critique against the new landing page and fixed everything it found: real page metadata (was shipping unedited "Lovable App" scaffold `<title>`/OG tags), a WCAG AA contrast failure on the accent colors against the dark background, zero `:focus-visible` styling anywhere in the project, and a non-technical fallback message for the hero stats when live data isn't reachable. Also designed a free hand-drawn SVG logo mark (`src/components/dl/logo.tsx` — a 2-1-3 podium on a track-lane arc, with a `light` variant for the sidebar's textured background) instead of paying for AI logo generation, wired in as the real favicon (`public/favicon.svg`).

**track-insights-main (dashboard/frontend), 2026-08-23, second round — user feedback: "you didn't actually use the skills, get rid of the emojis, and the dashboard needs a top nav instead of a sidebar, less boxy."** Real, substantive changes this round, not just installing skills:
- **Emoji removed end to end.** The 🥇🥈🥉🏅 markers were coming from `api.py`'s `build_top_winners()` (Python, not just the frontend) — fixed both sides. New `RankBadge` component (`src/components/dl/shell.tsx`) renders a colored circle using the same podium palette as the logo (gold/terracotta/brick for 1st/2nd/3rd, neutral gray for 4-6) instead of an emoji glyph.
- **Sidebar → top nav.** `src/components/dl/sidebar.tsx` deleted; `src/components/dl/topnav.tsx` is a new single horizontal bar (reuses the `track-surface` texture as a strip instead of a full-height panel) with the five nav links as pills. `Shell` (`shell.tsx`) no longer reserves 200px of left padding.
- **De-boxed**, per the (correctly-applied-this-time) `design-taste-frontend` skill's actual rule — "cards only when elevation communicates real hierarchy, otherwise group with spacing/dividers": the 4 separate bordered stat tiles on the dashboard are now one card with internal dividers; `Panel`'s outer border and header divider line are gone (relies on a subtle `bg-card`-vs-`bg-background` tone difference instead); Projections' 4 separate "storyline" boxes are now one card; all pill-style buttons (discipline pickers) are now `rounded-full`, matching the nav/CTA shape language instead of `rounded-md`.
- **Found and fixed 3 real pre-existing responsive bugs** while verifying at 375px (these were latent bugs surfaced by testing, not things introduced this round): the discipline table (6 fixed-width columns) blew out the page width on mobile — now wrapped in its own `overflow-x-auto` — and two multi-column grids (dashboard's winners/season-progress split, Projections' chart/confidence split) never collapsed to one column below `lg:`. All fixed and reverified.
- Verified via `tsc --noEmit` (clean), `eslint --fix` (clean, pre-existing warnings only), full Python test suite (37/37 passing), and live browser checks of all 6 routes at both 375px and desktop widths — the overflow bugs above were caught by actually testing, not assumed fixed from reading the code.

**track-insights-main (dashboard/frontend), 2026-08-23, third round — full `impeccable` dual-agent critique across all 6 routes, then fixed everything confirmed real.** Ran the skill's actual `critique` command this time (two isolated sub-agents: a design review + a detector/browser-evidence scan), synthesized into one report, persisted to `.impeccable/critique/`. Two of the detector agent's most alarming claims — top-nav overflowing at *both* mobile and desktop widths, and Projections' discipline picker desyncing from its own chart after in-app navigation — did **not** reproduce under direct live verification (nav only overflows at 375px, never at 1280px; the chip/chart pairing held consistent across every repro attempt, and the component code has no mechanism for the claimed leak — no shared/module-level state between routes). Documented as a real example of the project's own "a suspicious number is a signal to check harder" principle applying to agent-reported findings, not just model metrics. What *was* real and got fixed:
- **Systemic contrast failure, not just landing-page accents.** `--muted-foreground` (used on every table header, nat code, rank number, panel title, app-wide) measured 3.68:1 against white/card — failing WCAG AA's 4.5:1. Darkened `oklch(0.62 0.031 50)` → `oklch(0.5 0.031 50)` (~6:1). Separately, `--gold` used as text or as a background under white text (RankBadge's gold 1st-place badge, the landing hero CTA + step-number badges' gradient) measured 4.06–4.18:1, also failing. Added a new `--gold-strong` token (`oklch(0.52 0.128 68)`) used specifically wherever gold pairs with white as real text — `--gold` itself stays untouched for decorative/non-text uses (borders, dots, gradient stops) where the brighter hue and the 3:1 non-text threshold are both fine.
- **Mobile data loss, not just overflow.** Dashboard's Top Predicted Winners name column was collapsing to 51px (fixed-width mark/probability columns starved it); Projections' Contenders probability bar was rendering at literally 0px (invisible). Both restructured to wrap mark+probability onto their own line below `sm:` instead of squeezing beside the name — verified via `getBoundingClientRect()` that names now get 98–150px and bars render at real widths, with zero horizontal page overflow across all 6 routes at 375px.
- **Injury "Watch" badge** was exposing its reason only via a native `title` attribute (invisible on touch, unreliable for screen readers), and that reason is a raw scraped headline, not a structured injury description. Turned into a tap/keyboard-accessible popover (`aria-expanded`, dismissible) framed honestly as "Flagged from: <raw text>" instead of implying more diagnostic detail than the scraper actually has. The underlying extraction-quality problem (the injury_checker pipeline sometimes matches a meet-recap headline instead of real injury text) is a Python-side fix in the other repo — flagged, not silently treated as solved.
- **Top nav** gets a reused `.marquee-mask`-style fade (`nav-scroll-mask` in `styles.css`) on its scroll edges — real mobile-only overflow (438px of pills in a 198px window at 375px), now signaled instead of clipping abruptly with no cue Schedule/Projections exist.
- **Projections' discipline picker** was one flat 32-button row spreading Track+Field together, contradicting every other route's kept-separate pattern. Now grouped under "Track"/"Field" labels.
- Verified via `tsc --noEmit` (clean), `eslint` (clean, same 2 pre-existing fast-refresh warnings as before), and live DOM/contrast measurement (not screenshots — the Browser pane's screenshot compositor was unreliable this session, so verification used `getComputedStyle`/`getBoundingClientRect`/manual WCAG contrast math against the live page instead, which is arguably more precise anyway).
- Not yet committed/pushed as of this note — see git status before assuming this is live.

**track-insights-main (dashboard/frontend), 2026-08-23, fourth round — user feedback: "the dashboard/track/field pages still look ugly, use the other skills."** First tried `design-taste-frontend`, but that skill's own scope section explicitly excludes dashboards/dense product UI (it's landing-page/marketing-page focused, which is why it worked well on `/` earlier) — said so rather than forcing landing-page rules onto app UI. Switched to `emil-design-eng` (product-UI polish/animation philosophy) and `ui-ux-pro-max` (broader component/UX guidance that does cover dashboards) instead, both installed for this project per earlier rounds. Applied their guidance as real component changes, not a lookup-table default: `ui-ux-pro-max --design-system` returned a generic corporate-blue "Data-Dense Dashboard" preset that would have fought the existing terracotta/gold identity, so that preset was explicitly not applied — used the skill's targeted domain/checklist guidance instead of its generic system output. Concrete changes:
- **One shared `ProbabilityBar` component** (`shell.tsx`) replaced four separately hand-rolled flat-terracotta progress slivers (discipline-table rows, dashboard's top winners + season progress, projections' confidence list + contenders) — this was the single most-repeated "generic dashboard" element per the critique. Now a thicker, rounded, terracotta-to-gold gradient fill, animated on value change.
- **`RankBadge` (the podium-colored rank circle) now appears in the main discipline table**, not just Dashboard/Projections — it existed already but the app's primary data view (Track/Field) was still showing a plain gray rank number, an inconsistency nobody had caught. Also added to Projections' Contenders list.
- **Dashboard's 4 stat tiles** got a small hand-drawn line icon per stat (flag / calendar / grid / target, matching the landing page's existing no-icon-library icon pattern rather than pulling in a dependency) and a 3px colored top accent per tile, replacing four visually-identical bare-number blocks.
- **Discipline-picker pills** (Track/Field/Projections) switched from a flat terracotta active state to the same terracotta-to-gold gradient used on the landing page's CTA, plus real `:active` press feedback (`scale-[0.97]`) per Emil Kowalski's component-polish guidance (buttons should feel pressed, not just clicked).
- **WatchBadge's popover** now animates in from `scale(0.95)` + fade instead of appearing instantly (`@keyframes popover-in`, `prefers-reduced-motion`-gated) — small, but per the same guidance ("nothing in the real world appears from nothing").
- Verified via `tsc --noEmit` (clean), `eslint` (clean, same 2 pre-existing warnings), and live DOM checks (element counts, computed gradient/background values, `scrollWidth` overflow checks at 375px across all 4 touched routes) — the Browser pane's screenshot compositor is still unreliable this session (renders at a fraction of the real viewport), so visual verification leaned on DOM assertions rather than screenshots again.
- Not yet committed/pushed as of this note.

**track-insights-main (dashboard/frontend), 2026-08-23, fifth round — user feedback: "the opening screen and the production screen are so different, the dashboard/track/field pages look empty on the sides, make it more animated/interactive/polished using the skills provided."** Three distinct fixes:

1. **Theme flip: the dark landing-page palette is now the base theme everywhere**, not just on `/`. Asked the user directly which direction to close the light/dark split rather than guessing (`AskUserQuestion`) — they explicitly preferred the landing page's look and want the *app* to move toward it, not the reverse. `:root`'s `--background`/`--foreground`/`--card`/`--popover`/`--secondary`/`--muted`/`--border`/`--input` were redefined to the same dark values `.landing` already used (that page's own scoped tokens are untouched, zero risk there). Re-ran the full live WCAG contrast scan (same method as the third-round critique) against every app route post-flip and fixed everything it found: `--muted-foreground` flipped from a darkened-for-white gray back to a lightened-for-black gray (reusing the landing page's own already-verified value), a new `--gold-light`/`--terracotta-light` pair for gold/terracotta used as literal text directly on the dark background (mirrors the existing `--gold-strong` pattern but inverted — lighter, not darker), four hardcoded light-mode-only error boxes (`text-red-700`/`bg-red-50`, invisible-by-design against a light background, would've been low-contrast garbage on dark) swapped to the `--destructive` token, and a Recharts tooltip that was defaulting to a hardcoded white box (Recharts' own default, never themed) now explicitly styled with `--popover`/`--popover-foreground`. `--gold-strong` itself was intentionally left alone for RankBadge's gold-1st-place badge background, since that badge's internal text contrast is self-contained and doesn't depend on the surrounding page theme.
2. **"Empty on the sides" — `Shell`'s content container widened from `max-w-6xl` (1152px) to `max-w-[1600px]`** (`shell.tsx`), which was leaving 300px+ of dead margin on each side on anything wider than a 1280px laptop screen.
3. **A literal animated 400m running-track graphic** (`track-circuit.tsx`, new) now sits as a low-opacity background layer in the landing hero — five concentric lanes drawn as an actual stadium shape (two straights + two curves via SVG arc paths, not a plain oval), with a small gold marker continuously lapping the outer lane via CSS `offset-path`/`offset-distance` (`prefers-reduced-motion`-gated). Directly requested by the user ("make the landing page feel more track and field... a track circuit that's animated").
4. **General motion pass**, since "more animated, interactive" was a direct, separate ask from the theme/width fixes: a new `useCountUp` hook (`hooks/useCountUp.ts`, rAF-based, one-shot, reduced-motion-safe) animates Dashboard's 4 stat-tile numbers up from 0 on mount; a shared `.stagger-item` CSS class (`styles.css`) fades+lifts list rows in with a short per-item delay (`--stagger-i` custom property, capped at 10 items for the 32-row confidence list so a long list's cascade doesn't drag) applied to Dashboard's stat tiles + top-winners list, the discipline table's athlete rows (replays on every discipline switch, which reads as "new data arrived" rather than a bug), and Projections' contenders + confidence lists.
- Two false-positive contrast findings during the re-scan, both re-confirmed as scanner artifacts rather than real bugs (the scanner only walks the DOM ancestor chain for `background-color`, so it can't see a sibling-positioned overlay div darkening what's visually behind an element) — same root cause as a false positive caught during the third-round critique.
- Verified via `tsc --noEmit` (clean), `eslint` (clean, same 2 pre-existing warnings), a full live contrast re-scan of all 5 app routes plus the landing page (all clean after fixes), and `scrollWidth` overflow checks at 375px across every route (all clean). Screenshots remain unreliable in this session's Browser pane; verification leaned on DOM/computed-style assertions throughout, same as the last two rounds.
- Not yet committed/pushed as of this note.

**track-insights-main (dashboard/frontend), 2026-08-23, sixth round — user feedback: "the top nav border doesn't work anymore, would be better used somewhere else (especially the dashboard), and the whole thing still feels bland/flat, not a lived-in experience."** Didn't ask a clarifying question this time (unlike the theme-direction fork two rounds ago) — this was concrete craft feedback the agent is equipped to act on directly, not a strategic fork with two expensive, mutually-exclusive paths.
1. **`TopNav` dropped the brick/lane `track-surface` texture it had carried since the sidebar→top-nav conversion.** Repeating the same heavy textured strip as chrome on all six routes read as wallpaper, not a brand moment, once the user actually lived with it. Replaced with a plain translucent glass bar (`bg-background/85 backdrop-blur-md border-b border-border`) — the exact same treatment the landing page's own nav already used, so the two are now literally the same piece of UI rather than two different designs.
2. **The texture didn't disappear — it moved to a new Dashboard-only hero banner** (`Shell` gained an optional `hero` prop that replaces the plain `<h1>` title). Same structure as the landing hero (track-surface bg, dark overlay, content on top): a real title + one-line subtitle + the 4 stat tiles, which were also redesigned from boxed card-tiles (bg-card, divider grid, top accent bar) to bare items sitting directly on the texture (colored icon per stat instead of a bar), matching how the landing page's own hero stats are laid out. This was the "better place… especially the dashboard" the user asked for — a distinctive arrival moment on one page instead of repeated chrome on all of them.
3. **"Bland/flat" — added real depth, not just color:** `Panel` (used by every list/table on every route) gets back a subtle border + inset top highlight (`border border-border shadow-[inset_0_1px_0_rgba(255,255,255,0.05)]`) — a glass-card edge, the same idea the landing page's own cards already use; the earlier "de-boxing" round had deliberately dropped Panel's border for a light-theme flat look, which no longer reads as intentional now that the whole app is dark. Also added an ambient background layer at the `Shell` level (`ambient-glow`/`ambient-grain` utilities in `styles.css`, one `fixed pointer-events-none` div per page, not per-scrolling-element) — two very low-opacity terracotta/gold radial glows plus the same noise-grain texture already used in `track-surface`, so the page background reads as having depth instead of being a flat solid void.
- Verified via `tsc --noEmit` (clean), `eslint` (clean, same 2 pre-existing warnings), a full live contrast re-scan across Dashboard/Track/Projections (all clean — one more scanner false positive on the new Dashboard hero banner, same sibling-overlay-div cause as the last two rounds, confirmed via the same real-composite math: 7.18:1), and `scrollWidth` checks at 375px. The dev server survived an environment/session reconnect mid-round (a stray but still-live Vite process on port 8080, reattached rather than restarted) — HMR picked up all the edits made before the reconnect without needing a fresh start.
- Not yet committed/pushed as of this note.

**track-insights-main (dashboard/frontend), 2026-08-23, eighth round — user shared 4 reference images (a minimal B&W editorial landing, a pastel-light SaaS dashboard, a map app with glass cards over real terrain, a dark dashboard with one bold orange accent) and said "too dark, very dark" about the app.** Asked two follow-up questions rather than guessing a third time on visual direction (dashboard-specific vibe, then what "in-between" meant) — the user's answer: keep dark accents, want more light/off-white surfaces, "not very dark or very light," vibrant. This turned out to be a structural change, not a color tweak: the math showed a background at medium lightness (L 0.44) can't host readable text directly at all (dark text measured 2.3:1, and even the terracotta/gold accent hues measured under 2:1 as non-text elements, since the background is now the *same hue family* as the accents). So every page's text had to move onto an explicit light card/popover surface, with `--background` becoming a colorful CANVAS color that only shows in the gaps between cards, not something content sits on.
- **New palette** (`:root`, app-wide, landing untouched): `--background` → medium saturated terracotta (`oklch(0.44 0.09 40)`, ~`#7c3f2a`), `--card`/`--popover` → off-white (`oklch(0.97 0.012 75)`/`0.985`), `--foreground`/`--card-foreground`/`--popover-foreground` → dark warm near-black (`oklch(0.19 0.03 40)`), `--secondary`/`--muted` → light warm tan, `--muted-foreground` → mid-dark warm gray tuned for the new light card (7.88:1), `--destructive` → back near its original pre-dark-theme value (mid-lightness red reads fine on light card again), `--border` → solid warm gray (a translucent-white edge only made sense on a dark card). Computed real hex values for candidate palettes before committing (`oklch(0.44 0.09 40)` → `#7c3f2a`) rather than picking oklch numbers blind.
- **New `--terracotta-strong` token** (`oklch(0.45 0.164 38.5)`, 7.34:1 on the new card) — terracotta-as-text needed the same "darkened for a light surface" treatment `--gold-strong` already had. `--gold-strong` itself darkened further this round (0.52 → 0.49) after the live scan caught it failing (4.36:1) specifically on the "Final" badge's `bg-gold/15`-tinted surface, a lighter background than plain `--card`. `--gold-light`/`--terracotta-light` (the previous round's dark-surface-safe variants) weren't deleted — they're still correct for the Dashboard/landing hero banners, whose track-surface-textured overlay is still dark enough to need them (verified icon-only, >3:1 non-text; hero label/heading text switched to plain white/translucent-white instead, since even the light-accent variants measured under 4.5:1 against the now-lighter hero composite).
- **`TopNav` switched from a translucent dark glass bar to a translucent light one** (`bg-card/90`, was `bg-background/85`) — nav text is tuned for card surfaces now, and a medium-terracotta-tinted glass bar can't host it.
- **Every bare "loading..."/error message across all 5 app routes was sitting directly on the page background** (never wrapped in anything) — real, not cosmetic: measured contrast in the low 1s-2s once the background became colorful instead of a near-black neutral. Wrapped all of them in a card. Same fix applied to `Shell`'s default plain-title case (added a card wrapper there too) and to Projections' Track/Field discipline-picker group labels (were bare text in an unwrapped flex row).
- **`Panel`'s shadow flipped from an inset top-highlight to a real drop shadow** — the inset-white-highlight trick simulates a glass edge on a *dark* card and is invisible on a near-white one; a card floating on a colored canvas needs a shadow underneath it instead. New `card-shadow` utility (`styles.css`) shared across `Panel`, `Shell`'s title card, and every loading/error card, instead of repeating the same arbitrary shadow value everywhere.
- **Ambient glow/grain dialed back** — the previous round's colored glow made sense against a near-black background; against an already-saturated medium background the same opacities read as muddy patches, so the glow switched from terracotta/gold color to a much subtler light/dark pair for gentle depth variation, and grain opacity eased down slightly.
- **Found and fixed a real scanner bug mid-round, not just a background-image limitation**: `getComputedStyle().backgroundColor` returned `oklab(...)` instead of `oklch(...)` for one composited translucent color, which the contrast-scanner regex didn't match, causing it to silently walk past the real (correct) background to a grandparent's. Added `oklab()` parsing to the scanner. Also hit and correctly dismissed a Recharts-internal false positive: an off-screen text-measurement `<span>` Recharts appends directly to `<body>` for width calculation, positioned at `top: -20000px` — real DOM, real text, completely invisible to any user, not a bug.
- Two real (not false-positive) issues the re-scan caught and fixed this round: the Projections discipline-picker's "Track"/"Field" group labels had no background of their own (now wrapped in a card, matching the rest of the page); a `text-muted-foreground/70` opacity modifier that was fine against the old dark card dropped below 4.5:1 against the new light one (raised to `/85`, re-verified at 5.32:1).
- Verified via `tsc --noEmit` (clean), `eslint` (clean, same 2 pre-existing warnings), a full live contrast re-scan of all 5 app routes plus landing (all clean after fixes; landing itself untouched and still shows only its one known false positive), and `scrollWidth` checks at 375px across Dashboard/Track/Schedule/Projections (all clean). Screenshots were unavailable for a third distinct reason this session (the Browser pane wasn't displayed client-side) — verification was fully DOM/computed-style/math-based, same as every round since the compositor first became unreliable, but this round in particular leaned on computing real candidate hex values and full contrast tables *before* writing any CSS, not just checking after.
- Not yet committed/pushed as of this note. This is now five consecutive rounds of app-wide visual iteration in one session (dark flip → nav/depth pass → warm-up → this in-between pass) — worth git-committing checkpoints going forward if more rounds follow, so a bad direction is one `git revert` away instead of another full redesign.

**track-insights-main (dashboard/frontend), 2026-08-23, ninth round — user feedback: "the dashboard background feels stale/bland, add some grain and a track-curve illustration; and landing-to-dashboard still feels very different."** Two fixes, one of them closing out a complaint that had resurfaced across four separate rounds this session.
- **`ambient-grain` (Shell's shared background texture) made actually visible**: opacity `0.06 → 0.15`, and coarsened the noise (`feTurbulence baseFrequency 0.9 → 0.7`) so it reads as a worn/textured surface rather than fine digital static.
- **New `TrackCurveDecoration` component** (`track-curve.tsx`) — a quieter, static cousin of the landing hero's animated `TrackCircuit`: four concentric circles whose centers sit outside the SVG's own viewBox, so the visible slice naturally reads as a track's corner curve without hand-computed arc paths. Fixed in the bottom-right corner of the viewport at low opacity, behind all page content (`z-0`, `pointer-events-none`), wired into `Shell` so it's present app-wide, not just `/dashboard`.
- **Landing page's background unified with the app's**: `--landing-bg` now equals `:root`'s `--background` exactly (same medium terracotta) instead of staying near-black. This is the fourth time "landing vs. dashboard looks like two products" came up this session (original ask → dark-theme flip → "too dark" → "in-between" pass, which fixed the *app* but left landing untouched → this round) — decided to close it definitively rather than adjust again next round. Required re-verifying every landing-specific text token against the much lighter background: `--landing-fg` (white) still cleared easily (7.4:1), but `--landing-muted`/`--landing-accent-text`/`--landing-accent-text-gold` all fell under 4.5:1 and needed relightening.
- **Caught a second-order effect the first attempt at this missed**: initially lightened `--landing-card`'s glass-tint to a bright white at 10% opacity for visual definition against the new colorful background — but most landing text sits *on* that tinted card, not on flat `--landing-bg`, and the tint itself was lightening the effective background enough to fail contrast again (measured live: 4.0:1, 3.65:1, both new regressions from the first fix). Rather than keep chasing the text lighter and lighter (which was starting to wash the terracotta/gold accents out toward plain white), backed the card tint down to 3% opacity and re-verified against the *actual* card composite this time, not just the flat background — this is the same "check the real composited surface, not the token in isolation" lesson from the sibling-overlay-div scanner false positives earlier in the session, just self-inflicted this time instead of caught from an agent's report.
- Verified via `tsc --noEmit` (clean), `eslint` (clean, same 2 pre-existing warnings), a full live contrast re-scan of the landing page (clean) and Dashboard (clean, `TrackCurveDecoration` confirmed `pointer-events: none`/behind content), and `scrollWidth` checks at 375px on both. Did not re-scan Track/Field/Schedule/Projections this round since neither change touched those files' markup, only shared CSS tokens/utilities and `Shell` (which they already inherit correctly) — the prior round's clean scan of those routes still holds for anything this round could plausibly have affected.
- Committed (not yet pushed) as part of the tenth round below, once the user gave the explicit go-ahead.

**track-insights-main (dashboard/frontend), 2026-08-23, tenth round — user feedback: "center the top nav's Dashboard/Track/Field/Schedule/Projections links, keep the logo on the side, then commit."** `topnav.tsx`'s header switched from a flex row (`flex-1` nav soaking up leftover space, left-aligned) to a real 3-column grid (`grid-cols-[1fr_minmax(0,auto)_1fr]`: logo `justify-self-start`, nav in the middle, live-badge/timestamp `justify-self-end`) so the nav links are centered against the *whole bar*, not just centered in the leftover space next to an unevenly-sized logo.
- **First attempt broke mobile**: added `justify-self-center` to the nav grid item so it would visually center on desktop, but a grid item with `justify-self: center` sizes to its own content width and centers *within* its track rather than stretching to fill it — at 375px the nav's natural content width (438px) exceeded its 230px track, so the nav rendered 208px wider than the viewport and started at `x: -47.75` (partially off-screen), with the page 15px wider than the viewport. The fix: drop `justify-self-center`, let the item default to `justify-self: stretch` (filling the track exactly, which is what lets `overflow-x-auto` + `min-w-0` actually clip/scroll oversized content), and add `justify-content: center` instead so the *links* center within the now-track-width-constrained nav on desktop where they fit, while still scrolling correctly on mobile where they don't. Re-verified: desktop links centered to within 0.008px of the bar's true center, mobile nav correctly clips to its 230px track with internal scroll re-enabled, zero page-level horizontal overflow at 375/768/1280px.
- **Committed to track-insights-main** (`d8f958b`, not yet pushed) — this closes out ten rounds of same-session visual work: the theme flip, the nav/depth pass, the warm-up, the in-between palette pivot, the dashboard hero + grain + track-curve, the landing/dashboard unification, and this nav-centering fix, all in one commit (`Rework app theme to a warm medium-terracotta canvas with light cards`). The user explicitly scoped this commit to the frontend/dashboard work; asked separately before touching anything landing-page-specific further, since they flagged landing as needing "a little more work" next.
- Verified via `tsc --noEmit` (clean), `eslint` (clean, same 2 pre-existing warnings), and live geometry checks (not just contrast this time — `getBoundingClientRect` on the nav and its links) at 375/768/1280px.

**track-insights-main (dashboard/frontend), 2026-08-23, seventh round — user feedback: "now it looks too dark, very dark. I wanted a track and field aesthetic that's visually pleasing and creative, an experience."** The prior two rounds' dark tokens were technically correct (WCAG-passing) but nearly desaturated: `--background` was `oklch(0.16 0.025 40)` — chroma 0.025 is barely-there color, so despite a warm hue angle it read as generic near-black SaaS-dark-mode, not a track. Didn't ask a clarifying question here either — "too dark" plus "track and field, visually pleasing, creative" was concrete enough craft direction to act on.
- **Warmed and lightened every surface token** (`--background`/`--card`/`--popover`/`--secondary`/`--muted`, both in `:root` and in `.landing` so the two stay in sync): lightness raised (e.g. background `0.16 → 0.21`) and chroma roughly 2.5x'd (`0.025 → 0.06`) across the board. Same hue (40, the terracotta/clay angle), just actually visible now — reads as a dark clay/stadium-at-dusk tone instead of near-black. Computed the actual sRGB before committing (`oklch(0.16 0.025 40)` → `#160906`, essentially black; `oklch(0.21 0.06 40)` → `#340a01`-ish, a real warm brown) rather than guessing at the oklch numbers blind.
- **`--border`/`--input` warmed from neutral translucent white to translucent terracotta** (`oklch(1 0 0 / 0.1)` → `oklch(0.75 0.08 45 / 0.16)`), so panel edges read as part of the warm palette instead of a generic gray UI-kit line. Same change applied to `--landing-border`/`--landing-card`.
- **Boosted the ambient-glow/grain layer's opacity** (0.1/0.06/0.05 → 0.16/0.1/0.08) added last round, now that there's more color underneath for it to actually read against.
- Re-verified every text/background pairing against the new values **before** applying them (computed live in the browser, not guessed): foreground/muted-foreground/gold-light/terracotta-light/destructive all checked against the new background and card lightness, every one improved margin rather than just holding it (lightening the background only helps contrast against already-light text tokens). Applied once confirmed, then re-ran the full live contrast scanner across Dashboard/Track/Projections/landing post-change — same two known false positives as every prior round (Dashboard hero banner, landing hero — both the sibling-overlay-div scanner limitation, both re-confirmed via real-composite math: 6.54:1 and 6.68:1), nothing new.
- Verified via `tsc --noEmit` (clean), `eslint` (clean, same 2 pre-existing warnings), and `scrollWidth` checks at 375px (clean). Screenshots still unavailable this session — this time because the Browser pane wasn't displayed client-side ("the Browser pane is not displayed, so the page is not compositing frames"), a different failure mode than the earlier rounds' compositor-scaling bug. Verification leaned on DOM/computed-style assertions throughout, same as every round since the compositor first became unreliable.
- Not yet committed/pushed as of this note.

## Architecture / Key Files

- `run.py` — master pipeline: scrape live 2026 data → injury check → load trained model → build features → predict → `outputs/predictions_latest.csv`
- `api.py` — Flask bridge serving predictions + injury data to the dashboard (`/api/predictions`)
- `src/train_model.py` — retraining entry point. `--with-recency --with-h2h` for the full feature set; `--dry-run` to backtest without overwriting `outputs/`; `--tune` to grid-search hyperparameters
- `src/dl_final_results_scraper.py` — real DL Final ground-truth labels (`data/dl_final_results.csv`), scraped from WA's GraphQL API
- `src/season_results_scraper.py` — real per-meeting season history, enriches `data/raw/{discipline}.csv`
- `src/major_meets_scraper.py` — real per-meeting results from Olympics/Worlds/Continental Tour Gold/Euro Champs, also enriches `data/raw/{discipline}.csv`
- `src/historical_scraper.py` — season-best toplists 2018-2025 (`data/raw/{discipline}.csv` base layer)
- `src/live_fetcher.py` — scrapes current-season (2026) standings/toplists for live predictions
- `src/injury_checker.py` — injury/withdrawal detection + severity estimation (`data/injury_flags.json`)
- `src/h2h_calculator.py` / `data/h2h/h2h_rates.csv` — head-to-head win rates, a trained feature
- `tests/` — unit tests, `python -m pytest`
- `src/components/dl/shell.tsx`, `src/lib/dl-data.ts` (track-insights-main) — dashboard shell (page wrapper + `Panel`/`RankBadge`/`WatchBadge` primitives) + API data contract
- `src/components/dl/topnav.tsx` (track-insights-main) — the top navigation bar; replaced `sidebar.tsx` (deleted) 2026-08-23
- `src/routes/index.tsx` (track-insights-main) — the "/" landing page; `src/routes/dashboard.tsx` — the actual dashboard, moved here from "/"
- `src/components/dl/logo.tsx`, `PRODUCT.md` (track-insights-main) — PodiumCall's logo mark + durable product context for the `impeccable` skill

## Failed Attempts (tried, verified, did not help — don't redo without new evidence)

- **Event-specific models** (separate RandomForest per event group). Scored identically to one pooled model — `season_rank`/`season_percentile` already normalize per-discipline, so separate models add nothing.
- **`h2h_meetings` (count) as an additional feature** alongside `h2h_win_rate`: made results slightly worse. Not included.
- **Manual h2h blend at fixed weights**: tied with the trained-feature approach at best. Trained-feature chosen for architectural consistency (a manual post-hoc blend was the exact pattern that caused the recency-feature bug — didn't want to reintroduce it for h2h).
- **`src/scraper.py` (Wikipedia-based)**: fully non-functional, 0 rows ever. Deleted.
- **Including the DL Final's own results in `season_results_scraper.py`** (`rankingCategory in {"GW","DF"}`): produced a suspiciously large accuracy jump (63.8%) that turned out to be label leakage — the Final's own result was leaking into that year's training features. Fixed by restricting to `"GW"` only. **Lesson: a suspiciously good number is a signal to check harder, not a result to bank.**
- **Real per-athlete race-log scraping (all meets worldwide, not just the DL circuit)** (2026-08-23): confirmed via GraphQL introspection that WA's public API schema *has* fields for this (`getSingleCompetitorResultsDiscipline`/`getSingleCompetitor.resultsByDate`, taking a competitor id from `searchCompetitors`) — but the resolvers are dead server-side. Every combination of athlete id, year, and order-by argument tried (including with browser-matching headers) returned `resultsByDate: null`, while sibling fields on the same object (`activeYears`) resolved fine. `getSingleCompetitorSeasonBests` *does* work and confirms athletes' bests across all competitions (not just DL), but it's still one row per discipline per season — no more multi-meet granularity than what `season_results_scraper.py` already provides for the DL circuit specifically. **Don't re-attempt this without new evidence the resolver was fixed** — it's a dead field, not a query-construction mistake.

## Known Limitations (real, understood, not bugs to re-chase)

- `season_results_scraper.py` only covers the Diamond League circuit, not an athlete's entire season everywhere they raced. An athlete who mostly raced non-DL meets still shows thinner history than reality. Real per-athlete profile scraping would close this fully but WA's API doesn't currently expose it (see Failed Attempts above) — the only way to close this now would be scraping every meeting worldwide via the general calendar, a much bigger undertaking with real rate-limit/politeness concerns at that scale.
- `men_5000m` has real labels for only 1 of 5 years (2024) — other years ran a 5km road race or didn't contest it in a form the scraper recognized. Auto-detected from real data, not hand-flagged, but the practical effect is thin signal for this one discipline.
- 2020 alone is excluded from training labels (COVID-era "Inspiration Games" exhibition, not a real qualifying Final). 2018/2019 are now included — see the Ground Truth note above for why they turned out not to need special handling after all.
- Two specific qualified athletes (Agnes Jebet Ngetich, women's 5000m; Yemisi Mabry, women's shot put) are genuinely absent from World Athletics toplists even at top-500 depth. Not fixable via more pagination.
- Women's Shot Put / Women's 5000m predictions are consistently short by 1-2 athletes.
- Projections page's per-meet trajectory chart is fabricated interpolation, honestly labeled but not rebuilt — deliberately deprioritized by the user.
- Minor, not acted on: `api.py` hardcodes `"qualified": true` for every athlete (harmless no-op); `lovable-error-reporting.ts` does nothing outside the Lovable editor; React Query is wired in but `usePredictions.ts` still does manual `fetch`/`useState`.

## Next Steps

1. **If pushing accuracy further**: the easy levers are now applied — 2018/2019 label years, real per-athlete profile scraping (dead end, see Failed Attempts), and major non-DL meets (Olympics/Worlds/Continental Tour Gold/Euro Champs, plus a recognized-athlete noise filter) are all done (2026-08-23). None of it moved the headline number much (59.1%→60.3%→60.1%), which at this dataset size (~459 rows) may just be close to this feature set's ceiling. Remaining honest levers: scraping every meeting worldwide for true full-season per-athlete history (big undertaking, real rate-limit concerns), the other five Area Championships if the European-only scoping turns out too narrow, or finding genuinely new predictive features beyond the current 14.
2. **Test coverage**: done (2026-08-23) — `build_labeled_dataset()`/`train_and_backtest()`/`load_predictions()` are now covered end to end via `tests/fixtures/` + `tests/test_fixtures_integration.py`. If further expanding: `run.py`'s `build_2026_features()` and the injury-checker's scraping logic are still untested (both need Selenium/live network, harder to fixture).
3. **Polish, picked back up 2026-08-23**: landing page (done, round 1), branding/logo (done, round 1), sidebar→top-nav + emoji removal + de-boxing (done, round 2 — see "Uncommitted Right Now"). Mobile layout is now actually checked at 375px across all 6 routes (landing, dashboard, track, field, schedule, projections), not just the landing page. Still deprioritized, not yet started: READMEs for both repos, a real per-meet Projections chart (still fabricated interpolation, see Known Limitations), React Query refactor (`usePredictions.ts` still does manual `fetch`/`useState`). If the user wants another design pass, the installed `impeccable`/`design-taste-frontend`/Emil Kowalski skills are there — actually invoke them and follow their guidance (this session's lesson: installing a skill isn't the same as using it).
4. **Otherwise**: the system is in good shape 12 days out from the Final. Rerun `run.py` after Zurich (Aug 27) and again closer to Sep 4-5 to pick up final-season data — nothing else is currently broken or blocking.
