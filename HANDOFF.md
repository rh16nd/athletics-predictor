# PodiumCall (2026 Diamond League Predictor) — Handoff

_Last updated: 2026-08-24 (eighth session) — a long profile-page correctness pass, in order: a new real per-meeting scraper for the CURRENT season (`src/current_season_scraper.py`, 232/237 athletes now show real 2026 form instead of falling back to a prior season); two real, structural bugs found and fixed in `run.py`'s `build_2026_features()` — `days_since_last` was computed from an athlete's season-BEST mark's date (not their actual last race) and `meets_count` was structurally *always 1 for every athlete* (both silently relied on the live toplist snapshot, which only ever has one row per athlete); a full `run.py` refresh run at the user's request to make `win_probability` and every other model-fed field fully consistent with the fixes, not just the display (Rai Benjamin: stale rank #4/2% → real rank #2/14%, season best correctly updated to his faster 23 Aug mark); `src/refresh_current_season_stats.py` added as a cheap way to re-apply the `days_since_last`/`meets_count` fixes between full refreshes without a ~1hr rescrape; two small profile-page label fixes (Age rounds to a whole number, "Last raced" renamed "Last competed" for field events); the Track/Field discipline table spacing fix (zero gap between every column, read as congested); the "Q" qualified badge made real (was a hardcoded no-op); and a git-tracking cleanup (`outputs/predictions_latest.csv`/`data/standings.json` were gitignored but still tracked from an old commit — untracked via `git rm --cached`, files themselves untouched). Prior session (2026-08-23, seventh): profile-page polish — table-view toggle on both charts, profile links added to the Projections page, a real photo-lookup bug fix, real per-photo face detection (OpenCV DNN) replacing the fixed-percentage photo crop, a real back-navigation fix (the profile page's back link was hardcoded to `/dashboard` — now uses real browser history, with the Track/Field discipline-tab selection moved into the URL so it survives the round trip), and a real per-athlete season-history bug fix (was picking the dataset's global most-recent year instead of each athlete's own). Before that (same day, sixth session): landing page polish round via the impeccable + animate skills; three accuracy experiments that all regressed and were reverted (see Failed Attempts); then a new real athlete profile page (charts, real photo, real head-to-head) replacing the earlier inline expand panel._

## Goal

A fully automated ML prediction system for the 2026 Diamond League Final (Brussels, Sep 4-5), branded **PodiumCall**. It scrapes live World Athletics data, checks for injuries/withdrawals, runs a trained model, and serves win-probability predictions to a dashboard — with the explicit standing principle that everything shown is real, sourced data (scraped marks, real ground-truth results, honest walk-forward accuracy), never fabricated, hand-typed, or placeholder content standing in for something real. Two repos:

```
C:\Users\rayen\athletics-predictor\   ← Python ML pipeline
C:\Users\rayen\track-insights-main\  ← React dashboard (TanStack Router + Vite), "PodiumCall" front end
```

**Priority order, per the user**: model accuracy and data honesty first; visual/UX polish was deliberately saved for last, then explicitly picked back up 2026-08-23 and given a full session of iteration (branding, landing page, a design critique pass, a complete theme rework, and — same day — a real athlete profile page with charts; see track-insights-main's Current State below). **Accuracy work is now considered thoroughly investigated, not abandoned**: five real levers beyond the original wins were tried this session and last, all reverted, each with an understood root cause rather than just "didn't work" — see Failed Attempts and Next Steps #2. Treat 60.1% as the honest, well-tested ceiling for this dataset/feature-set combination unless a genuinely new idea (not a variant of something already tried) comes up. When in doubt about what to work on next, prefer real data over hardcoded/hand-typed data, and always isolate a change's effect before trusting a number that moved.

## How to Run

```powershell
# Terminal 1 — Flask API
cd C:\Users\rayen\athletics-predictor
venv\Scripts\activate
python api.py   # http://localhost:5000

# Terminal 2 — React dashboard
cd C:\Users\rayen\track-insights-main
npm run dev     # http://localhost:8080 (or 8081 if 8080 is taken)

# Refresh live predictions (scrapes World Athletics, ~1hr — covers all 32 disciplines + injury check)
cd C:\Users\rayen\athletics-predictor
venv\Scripts\activate
python run.py

# Rebuild real DL Final ground-truth labels (~1 min, hits a public WA API)
python src/dl_final_results_scraper.py

# Enrich training data with real per-meeting season history (~few min)
python src/season_results_scraper.py

# Add real results from Olympics/Worlds/Continental Tour Gold/Euro Champs (~few min)
python src/major_meets_scraper.py

# Refresh the CURRENT season's real per-meeting results (~few min) -- powers the
# athlete profile page's "Real Season Form" chart with this year's actual
# in-season marks instead of falling back to an athlete's last completed
# season. Re-run this periodically as new DL meetings happen (not part of
# run.py's own refresh, since it's independent of the prediction pipeline).
python src/current_season_scraper.py

# Cheap fix for outputs/predictions_latest.csv's "days since last competed"
# and "meets this season" stats between full run.py refreshes (seconds, no
# scraping) -- uses current_season_scraper.py's already-scraped data; does
# NOT update win_probability or other model-fed values, only run.py does that.
python src/refresh_current_season_stats.py

# Retrain the model (fast, seconds — uses already-scraped data/raw/*.csv)
python src/train_model.py --with-recency --with-h2h

# Grid-search hyperparameters via walk-forward folds (informational, prints only)
python src/train_model.py --with-recency --with-h2h --tune

# Run the test suite
python -m pytest
```

Both dev servers auto-reload on code changes (Flask debug mode, Vite HMR) and re-read data files fresh on every request — no restart needed after `run.py` or a retrain.

## Current State

**Model**: RandomForestClassifier, `n_estimators=200, max_depth=16, min_samples_leaf=1, class_weight=None` (`DEFAULT_MODEL_PARAMS` in `train_model.py` — walk-forward tuned via `--tune`, re-tune whenever the feature set, training-set size, OR data-quality filtering changes, since the winning config shifts each time — it's flipped `class_weight` None↔"balanced" twice across five re-tunes so far; hyperparameter search on a dataset this size (~459 rows) is itself somewhat noisy, so don't read too much into which single config "won" a given round). 14 features, **all carrying real signal**:
`season_best, career_best, pb_gap, meets_count, consistency, yoy_improvement, age, season_rank, season_percentile, weighted_season_best, wind_adj_season_best, recent_trend, days_since_last, h2h_win_rate`

**Backtest accuracy: 60.1%** (276/459) — walk-forward validated: trained on every year strictly before each test year, scored independently on 2021, 2022, 2023, 2024, and 2025 (5 folds, not one fixed holdout). The deployed model is refit on all 7 label years after validation. Trained on all 32 live-predicted disciplines (`men_5000m` has thin signal — see Known Limitations). This is essentially flat vs. the same-day earlier number (60.3%) despite two real additions since (major meets beyond the DL circuit, and a noise filter) — see the Failed Attempts / project memory for why that's an expected outcome of this dataset's size, not a wasted afternoon.

**Ground truth is real, not hand-typed.** `src/dl_final_results_scraper.py` pulls actual Diamond League Final results (2018-2025, excluding 2020) directly from World Athletics' own public GraphQL API — the same API the site's own frontend uses (`x-api-key` is a public key shipped in every page load, not a secret). It finds each year's Final by filtering `rankingCategory == "DF"` and reads which disciplines were contested (and under what name — e.g. "Mile" some years instead of "1500 Metres") directly from what's present in the response, rather than a hand-maintained list. **2018/2019 were initially assumed to need a different scoring format** (the Final was split across two meetings, Zurich + Brussels) and skipped — checking the actual per-meeting data (2026-08-23) showed each of the 32 disciplines' DF group appears at exactly one of the two meetings, never both, so it's a two-city Final, not a split score. The scraper now aggregates across however many DF meetings a year has instead of assuming exactly one. 2020 (COVID-era "Inspiration Games" exhibition) is still deliberately excluded.

**Training features go beyond a season-best toplist.** `src/season_results_scraper.py` pulls every *regular-season* Diamond League meeting's results (not just the Final — that would leak the label) for 2018-2025 (excluding 2020), giving real multiple-marks-per-athlete-per-season data. This is what makes `meets_count`/`consistency`/`recent_trend` real features instead of structural zeros.

**Beyond the DL circuit: `src/major_meets_scraper.py`** (added 2026-08-23, user-requested "meets with the big athletes") adds real per-meeting results from the Olympic Games, the senior outdoor World Athletics Championships, World Athletics Continental Tour **Gold tier only** (`rankingCategory == "A"`, filtered out of a much larger Silver/Bronze/Challenger pool), and the European Athletics Championships (the other five Area Championships — African/Asian/American/Oceania/NACAC — were deliberately left out per the user, quality varies too much by continent). These groups were found via `getCalendarEvents`'s own `options.competitionGroups`/`rankingCategories` introspection, not guessed.

**Both per-meeting scrapers filter to "recognized" athletes only** (`dl_final_results_scraper.load_recognized_names`): a row is kept only if that athlete appears somewhere in the discipline's own toplist (any year, not just that exact year — deliberately lenient about single-year toplist gaps). This exists because a big meeting can still have a weak field in a discipline that isn't its main draw (e.g. a field-filler making a Continental Tour Gold meeting's javelin final) — keeping those marks would add noise (thin, one-off meets_count/consistency signal) rather than real information about the athletes DL Final prediction cares about. Drop rates are meaningful (e.g. women_800m dropped 98 of 254 candidate major-meet rows) — this is doing real work, not a no-op filter.

**Injury/withdrawal detection is fully wired end-to-end.** `src/injury_checker.py` scrapes news + meet-results recaps for injury/DNF signals, estimates recovery time, and either flags ("watch") or drops ("remove") an athlete from predictions. Both outcomes are visible on the dashboard: a "Watch" badge (linking to the real evidence) on flagged athletes, and a "Removed from predictions" panel (shown only when non-empty) for dropped ones.

**Test suite**: `tests/` — pure-function unit tests for `train_model.py`, `api.py`, `dl_final_results_scraper.py` (`python -m pytest`, no network/Selenium/Flask/real-files needed), plus `test_fixtures_integration.py` covering `build_labeled_dataset()`/`train_and_backtest()`/`load_predictions()` end to end against small checked-in fixtures (`tests/fixtures/`) instead of real scraped data. 37 tests total. Includes regression tests for the specific bugs described below.

**Both repos are pushed and up to date** — track-insights-main `49a425e` (real athlete profile page, see the dedicated Current State section below; landing-page polish is the earlier `a8f0d7b`), athletics-predictor `ab909aa6` (`/api/athlete` endpoint + real per-athlete stats + a real photo lookup, see below; no Python accuracy changes survived this session — see Failed Attempts).

**track-insights-main (dashboard/frontend) — consolidated 2026-08-23 end-of-session.** Ten rounds of visual work happened in a single session; the round-by-round blow-by-blow has been folded into the current-state summary below (full history is in git log / commit `d8f958b` if ever needed — nothing below is lost, just no longer duplicated as a chat transcript).

*Branding & structure*: product is **PodiumCall** (domain-checked, real hand-drawn logo at `src/components/dl/logo.tsx` — a 2-1-3 podium on a track-lane arc). Landing page lives at `/` (`src/routes/index.tsx`); the actual dashboard moved to `/dashboard`. Navigation is a single top bar (`src/components/dl/topnav.tsx`, replaced an earlier left sidebar), not a sidebar — 5 links (Dashboard/Track/Field/Schedule/Projections), centered in a true 3-column grid (logo left, nav center, live-badge/timestamp right) so the links are centered against the whole bar regardless of how wide the side content is.

*Theme (current, final state)*: one shared visual language across landing **and** app — a genuinely medium-lightness, saturated terracotta **canvas** color (`oklch(0.44 0.09 40)`, ≈`#7c3f2a`) that page backgrounds sit on, with actual content living on off-white **cards** (`oklch(0.97 0.012 75)`) with dark text (`oklch(0.19 0.03 40)`). This took several iterations to land on (near-black → warmed-dark → "too dark" feedback → this medium-canvas-with-light-cards pass → landing unified onto the same canvas color) — the durable reason it ended up this shape: a background this light genuinely cannot host readable text directly (measured contrast in the 2s), so every page's real content had to move onto an explicit light card/popover surface, with the colorful canvas only showing in the gaps between cards. Key tokens in `styles.css`: `--background`/`--card`/`--popover` (canvas/card/popover surfaces), `--foreground`/`--muted-foreground` (dark text tuned for the light cards), `--gold-strong`/`--terracotta-strong` (darkened accent-as-text, safe on light cards), `--gold-light`/`--terracotta-light` (lightened accent-as-text/icon, safe only on the two dark textured hero banners — see below), `--border` (solid warm gray in the app, translucent white-tinted in landing's glass cards). The landing page (`.landing` class scope) keeps its own token set because it composites things (hero texture+overlay, translucent glass cards) the rest of the app doesn't, but the base color values are hand-kept in sync with `:root` so the two read as one product.

*Distinctive visual elements*: the Dashboard has its own hero banner (`Shell`'s `hero` prop) built on the `track-surface` texture utility (brick + lane-line pattern) with a dark overlay, title, subtitle, and the 4 stat tiles (count-up animated via `useCountUp`, `hooks/useCountUp.ts`) sitting directly on it. The landing hero has an animated `TrackCircuit` (`track-circuit.tsx`) — a real stadium-shaped 400m track (two straights, two curves, five lanes) with a marker continuously lapping the outer lane via CSS `offset-path`. The whole app shell (`Shell` in `shell.tsx`) has an `ambient-glow`/`ambient-grain` textured background layer plus a static `TrackCurveDecoration` (`track-curve.tsx`, a quieter corner-curve illustration) sitting behind all page content. Shared components worth knowing about: `Panel` (bordered, shadowed card used for every list/table), `RankBadge` (podium-colored rank circle, replaced emoji), `ProbabilityBar` (shared gradient progress bar, replaced 4 separate hand-rolled ones), `WatchBadge` (tap/keyboard-accessible popover for injury-flag reasons, replaced an inaccessible `title`-attribute tooltip).

*Working methods worth repeating next time this needs more visual work*:
- The Browser pane's screenshot capability was unreliable all session (three different failure modes hit across the day). Verification instead used live `getComputedStyle`/`getBoundingClientRect`/contrast-math checks run via `javascript_tool`, which turned out to catch real bugs a screenshot glance might have missed (e.g. a scanner bug where `getComputedStyle` returned `oklab(...)` instead of `oklch(...)` for one composited color).
- When changing a background/surface token, always recompute contrast against the **actual composited surface** real content sits on (translucent tints, glass-card fills, gradients) — not the token in isolation. Got burned by this twice: once from an agent-run critique's findings, once self-inflicted (lightened a glass-card tint for definition, didn't realize the tint itself was lightening the effective background enough to fail text contrast again).
- Ask a clarifying question before a big, expensive, genuinely-forking visual decision (e.g. "should the app move toward the light theme or the dark one" had two mutually-exclusive expensive answers). Don't ask for craft-level feedback the agent can just act on directly ("this feels bland," "move the nav texture") — those got acted on directly, no question needed, and that was the right call each time.
- CSS grid gotcha: `justify-self: center` on a grid item sizes it to its own content and can overflow the track; if the item also needs to shrink/scroll (e.g. a nav with `overflow-x-auto`), leave `justify-self` at its default `stretch` and center the *inner content* with `justify-content` instead.
- The Browser pane's screenshot problem turned out to be one symptom of a bigger issue (found 2026-08-23, sixth session): its automation does not advance the page's animation/compositor clock **between separate tool calls** — a CSS transition triggered in one `javascript_tool` call and checked via `setTimeout` in a later call always reads as frozen at 0%, and a real `IntersectionObserver` set up and awaited across calls with `setTimeout` never fires. Both work fine once pumped with `requestAnimationFrame` loops **inside a single script execution** (confirmed via `element.getAnimations()`/WAAPI and a raw observer test) — so verifying any scroll-triggered or transition-based motion in this environment means doing the whole trigger-and-check sequence in one `javascript_exec` call, not across several.

*Status*: both repos committed and pushed (see the pushed-commit line above). This session's landing-page round covered visual polish (craft-floor fixes) and motion; the user hasn't given further specifics for landing-page work beyond this — ask before assuming a direction if picking it back up.

**Athlete profile page — new this session (2026-08-23, sixth session, same day as the above).** Started from a user request to surface real-but-unused data (WA's `Pos`/finishing-position column, `h2h_rates.csv`'s per-matchup detail, WA's own `Results Score`) as user-facing info rather than only hidden model inputs, referencing outlier.bet as a "per-player stats" inspiration. Went through several rounds on the same page:

- **Round 1** was an inline click-to-expand panel under each Dashboard row (real per-meet history + h2h as a text list). User feedback: "looks bland," wanted a real dedicated page with actual charts instead — that inline panel was fully removed in favor of what's below, not kept as a secondary view.
- **Round 2** built the real page: new route `/athlete/$discKey/$name` (TanStack Router dynamic segment), reached from the Dashboard's top-winners list and every Track/Field discipline table row (those previously linked straight to the external WA profile; that link now lives inside the new page instead). New Flask endpoint `/api/athlete/<discKey>/<name>` (`api.py`'s `build_athlete_profile()`), computed lazily per page view, not embedded in the bulk `/api/predictions` payload. Two real charts built per the `dataviz` skill's procedure (form before color, direct labels used sparingly, single y-axis, no fabricated interpolation): `SeasonTrendChart` (real per-meet marks from the athlete's last *completed* season only — 2026's own toplist is one row per athlete, so there's nothing true to trend yet this year) and `HeadToHeadChart` (real win/loss bars vs. the discipline's other top picks). `run.py` now also exports `career_best`/`pb_gap`/`age`/`meets_count`/`days_since_last` to `predictions_latest.csv` — all already computed for the model but previously discarded before hitting disk.
- **Round 3**: user asked for more visual polish/"charts," referencing dark-dashboard mood-board images, plus a real athlete photo. Explicitly did **not** reskin the app dark (that contradicts this project's own settled, multi-round-iterated terracotta/light theme — flagged to the user rather than silently pivoting); took the *pattern* (donut meters, icon stat tiles, layered depth) instead. Added `RadialMeter` (circular win-probability gauge, replacing the linear bar) and icon-labeled stat tiles. **Real athlete photo**: found via live GraphQL schema introspection that `getAthleteActionPictureByIds(ids: [competitorId])` resolves a real image on World Athletics' own asset CDN (`https://assets.aws.worldathletics.org/<primaryMediaId>`) — confirmed by fetching a real WA profile page's HTML and matching its own `og:image` tag byte-for-byte. `api.py`'s `load_athlete_photo()` does the lookup; returns `None` (never a stock photo/generic avatar) if no real photo resolves, and the frontend falls back to an initials monogram in that case.
- **Round 4**: user wanted the photo spanning the whole hero (not a small circle) and a bigger meter. First attempt used a flat single-opacity tint strong enough to guarantee worst-case text contrast (measured, not eyeballed) — but that muddied the photo too much to actually see it. **Round 5** (user: "make the photo a bit more clearer") replaced the flat tint with two lightweight scrims: a light uniform brand tint everywhere (barely dims the photo) plus black gradients anchored *only* at the top and bottom edges (protecting the back-link and the name/meter respectively), both fading to nothing well before the vertical middle — the actual photo subject stays clearly visible while text keeps a verified-safe contrast margin (13.9:1 / 10.4:1 / 5.3:1 for name/subtitle/back-link against a synthetic worst-case near-white photo patch).
- **A real bug was found and fixed along the way**: pandas/numpy scalar types (`int64`/`float64`) aren't JSON-serializable as-is, which 500'd the new endpoint the first time it was hit live — caught by testing the actual endpoint, not assumed working from reading the code.
- **Known caveat, not a bug**: WA's raw photo asset is often a wide action-shot banner (e.g. 1500×535), not a pre-cropped square headshot — `object-fit`/`background-position` handles this reasonably (centers on the subject) but won't always frame a face as tightly as a purpose-cropped avatar would.
- **Table-view toggle added (2026-08-23, seventh session).** Both profile-page charts (`SeasonTrendChart`, `HeadToHeadChart`) now have a "Table view"/"Chart view" button (`aria-pressed`, matches the existing `label-caps`/bordered-button visual language) that swaps the SVG/bar visualization for a real `<table>` of the same underlying data (season trend: Date/Mark/Venue/Score; head-to-head: Opponent/Wins/Losses/Meetings), each with a `sr-only` caption. This was the one item from the dataviz skill's accessibility checklist ("a table view exists") not yet done when the profile page shipped. Verified live via `javascript_tool` DOM clicks (the Browser pane's screenshot/coordinate-click path is still unreliable per the working-methods notes below — clicking by `ref` coordinates silently no-op'd here, `element.click()` via JS worked and was used to confirm both toggles + real table contents).
- **Profile-page reach + photo quality, seventh session (user-reported: "why is the profile only available for the top predicted winners", "some athletes don't have photos", "photos should show their faces").** Investigated all three:
  - **Reach**: the profile page was already linked from every Dashboard/Track/Field row, not just the Dashboard's 6-athlete "Top predicted winners" list (confirmed live — e.g. clicked into a rank-#5 Track row never shown on the Dashboard). The real gap was **Projections** (`src/routes/projections.tsx`): the trajectory-chart leader name and the "Contenders" list (5 athletes/discipline) were plain text with no link. Both now link to `/athlete/$discKey/$name`, same pattern as `discipline-table.tsx`. **Deliberately left unlinked**: the Dashboard's "Removed from predictions" (injury/withdrawal) list — those athletes are, by definition, filtered out of `predictions_latest.csv` before `build_athlete_profile()` ever sees it, so a link there would 404 into the page's error state; fixing that would need a separate data source for withdrawn athletes, not attempted.
  - **Missing photos**: audited all 235 unique athletes in `predictions_latest.csv` live against `load_athlete_photo()` — **149 resolve a real photo, 86 don't (63%/37%)**. Root-caused two distinct reasons, only one a bug: (a) **real bug, fixed**: `load_athlete_photo`'s regex only matched WA's `.../athletes/athlete=<id>` URL format; ~8% of profile_urls (19/235, mostly newer/junior/collegiate athletes like Cooper Lutkenhaus, Jakobe Tharp) use a second real WA format, `.../athletes/<country>/<slug>-<id>`, which silently short-circuited to `None` without ever querying the API. Regex now also matches `-(\d+)/?$`. **Re-tested after the fix: still 0/19 resolve a photo** — WA's own system genuinely has no action picture on file yet for that specific cohort (confirmed by letting the query actually run instead of skip). The fix is still correct/kept (removes a false-negative shortcut, will pick up photos for this URL format the moment WA adds them) but doesn't move today's numbers — reported to the user as a real fix with an honest zero-visible-impact-today caveat, not oversold. (b) the remaining ~67 misses are athletes where the live `getAthleteActionPictureByIds` query genuinely returns no `primaryMediaId` — an honest "no photo on file," not fixable from this endpoint.
  - **Faces cropped out of frame**: verified the user's complaint was real and reproducible, not a taste call. `object-position`/`background-position` was `center 30%`; downloaded and locally re-cropped 10 real athlete photos (via PIL, mirroring the exact CSS `cover` math against the real hero container size, `1169×260`) spanning sprints/hurdles/distance/jumps/throws. At 30%, Noah Lyles' actual shipped photo cropped out his entire face (nose-up gone) while a bystander runner's face stayed fully in frame — a real, demonstrable bug, not a one-off. Tested 0%/15%/30%: **15% keeps the face in frame for 9 of the 10 real sample photos** (the one exception, a high-jumper photographed mid-air with her head tilted back, has no crop position that would show a normal face-on view — a photo-selection limit, not a positioning one). Shipped `center 15%` as a first pass — flagged as a heuristic, not real detection.
- **Real per-photo face detection, same day, immediate follow-up.** The `15%` heuristic still looked wrong to the user specifically on the desktop browser (a much wider, more aggressively-cropped hero banner than the narrower mobile layout, where a single fixed vertical percentage can't be right for every photo's actual composition — a portrait-mode photo, an off-center action shot, and a tight pre-cropped headshot all need a different crop). Replaced the fixed constant with **real face detection on the real downloaded photo**: `api.py`'s new `detect_face_focus()` runs OpenCV's res10 SSD face detector (`cv2.dnn`, Caffe model) against the actual photo bytes and returns the detected face's own `{x, y}` position as percentages, fed straight into `background-position` — so the crop follows wherever the face actually is, correctly, at *any* container aspect ratio (mobile or desktop), not just the one size it was tuned against.
  - **Model choice**: tried a plain Haar cascade first (bundled free, no download) — it produced a real false positive on Leonardo Fabbri's shot-put photo (locked onto his jersey/bib pattern at the bottom of frame, nowhere near his actual face, which is top-left). Switched to OpenCV's classic res10 SSD Caffe model (`deploy.prototxt` + `res10_300x300_ssd_iter_140000.caffemodel`, ~10.7MB, OpenCV's own canonical model, downloaded once and cached to `data/models/` the same on-demand pattern `webdriver-manager` already uses elsewhere in this project) — correctly found Fabbri's real face with no false positive.
  - **Multi-face disambiguation**: Noah Lyles' photo has two real faces in frame (him mid-celebration + a competitor beside him). Picking by raw detector confidence picks the *wrong* one here (the calmer bystander's face scores higher confidence, 0.80 vs 0.64, than Lyles' own contorted celebrating face) — switched to picking the **largest** detected face by box area instead, which correctly picked Lyles (the closer, more prominent figure) in this case and is the more reliable signal in general for "who is this photo actually of."
  - **Caching**: each photo is downloaded and run through the detector once per unique photo URL, then cached to `data/photo_focus_cache.json` (gitignored, regenerable) — repeat profile views don't re-fetch or re-detect. First-view cost measured live: ~1.5–2.5s per uncached photo (dominated by the photo download itself, not the detector, which runs in well under 100ms). A cached `null` means detection genuinely ran and found no confident face (distinct from "not computed yet") — those cases (verified live: Matthew Denny, Oleh Doroshchuk, Andy Díaz Hernández, Julien Alfred, Yaroslava Mahuchikh, Tara Davis-Woodhall — mostly side-profile or mid-air shots) fall back to the `center 15%` default rather than a wrong guess.
  - **New dependency**: `opencv-python-headless==4.10.0.84` added to `requirements.txt` (pinned specifically — the newer 5.x line dropped the bundled Haar cascade data files entirely, ships only the newer YuNet ONNX detector API instead; 4.10 still bundles what this needs and installs cleanly on Windows). `data/models/` and `data/photo_focus_cache.json` are both gitignored (regenerable on first use, same treatment as this project's other derived data files).
  - Verified live end-to-end (not just the offline PIL simulation): hit the running Flask dev server's `/api/athlete/men_100m/Noah%20LYLES`, confirmed `photoFocus: {x: 62.8, y: 10.7}` in the real response, confirmed the browser's live `getComputedStyle` on the hero div showed the matching `background-position`, then re-simulated that exact crop via PIL at both the real desktop (`1169×260`) and real mobile (`327×324`, measured live from the Browser pane with `resize_window` explicitly set to its `mobile` preset, `375×812` viewport) hero container sizes — both show Lyles' full, clear, in-frame face, a dramatic improvement over the pre-fix crop that cut it off entirely on desktop specifically (the user's exact complaint: looked fine on the narrower mobile layout, broken on the much-wider desktop banner).
- **Back-navigation fix, same session, immediate follow-up (user-reported: clicking an athlete from Track's 100m table and hitting back landed on `/dashboard`, not back to the 100m table).** The profile page's "back" control was a hardcoded `<Link to="/dashboard">` regardless of entry point. Fixed with TanStack Router's `useCanGoBack()`/`useRouter().history.back()` — real browser-history back when the app has somewhere to go back to (label becomes "← Back"), falling back to the old hardcoded `/dashboard` link only when there's no history to go back to (e.g. the profile page was opened directly via URL — label stays "← Back to dashboard" in that case, so it's never a dead-end-looking bare "Back"). **A second, less obvious bug surfaced while testing this fix**: even with real history.back(), landing back on `/track` reset the discipline-tab picker to its default (Men's 100m) — the active tab was local `useState` inside `DisciplineTable`, which remounts fresh on navigation and doesn't survive a history round-trip. Fixed by moving that selection into the URL itself: `/track` and `/field` now carry `?disc=<id>` as a real TanStack Router search param (`validateSearch`, `Route.useSearch()`, tab clicks call `navigate({ search: { disc: id }, replace: true })`), and `DisciplineTable` became a controlled component (`activeId`/`onActiveChange` props instead of internal state). Verified live end-to-end via `javascript_tool`: clicked "Men's 200m" → clicked into an athlete → clicked "← Back" → landed on `/track?disc=men_200m` with the Men's 200m panel still showing, matching the user's actual "browse through the athletes" use case, not just a URL-level fix.
- **Per-athlete season-history fix, same session (user-reported: season form should show "the most recent season," not hardcoded 2025, because some athletes didn't race the big competitions much in 2025).** Root cause in `api.py`'s `load_athlete_history()`: `last_year = int(df["year"].max())` used the *dataset's* global most-recent year for every athlete, then filtered that one athlete to only that year — so an athlete with zero 2025 rows in this DL-circuit-only dataset (hurt, skipped the circuit, focused elsewhere) showed "No prior-season meet history on record" even when they had a real, fuller season on file from an earlier year. **Confirmed live before fixing**, not assumed: audited all 237 predictions-list athletes, found 8 genuinely broken this way, including real notable names — Rai Benjamin (real 2024 flat-400m race hidden), Shaunae Miller-Uibo (real 2022 season hidden), Mutaz Essa Barshim (real 2024 season hidden), Nina Kennedy (real 2024 season, 8 races, hidden). Fixed by computing `last_year` from *that athlete's own* rows (`mine["year"].max()`) instead of the whole file's. Re-audited after the fix: all 8 now resolve their real season; the remaining empty cases (4 of 237) are athletes with genuinely zero history rows anywhere on file — a real, not-fixable-here limitation, not a bug.

**Track/Field table spacing + real "Q" badge, eighth session (2026-08-24).** Two related asks in one message: the discipline table "looks congested," and a conceptual question — "if an athlete hasn't run the Diamond League, they shouldn't be a predicted finalist, right?" (prompted by seeing Rai Benjamin, who the user knew mainly as a flat-400m runner, show up in that discipline's predictions).

- **Investigated the qualification question first, before touching any code**, since it's a correctness/trust question, not a cosmetic one. Traced the pipeline: `run.py`'s per-discipline prediction loop already *does* restrict to real WA-scraped 2026 Diamond League standings (`data/standings.json`, built by `src/live_fetcher.py`'s `scrape_dl_standings()`) whenever that data exists for a discipline — confirmed live that `men_400m`'s real standings list genuinely includes Rai Benjamin (8 real names, matching exactly what's shown), so this specific case was correct, not a bug: he really has competed in the 2026 DL 400m. Checked all 32 disciplines' `standings.json` entries live — none are currently empty, so every discipline's predictions are standings-based today, not the fallback.
- **But found a real, silent latent risk while tracing this**: when `standings.json` has no entry for a discipline (a scrape gap, or too early in a season before a standings page exists), `run.py` was already falling back to ranking by worldwide season-best time/mark instead — which does **not** verify real Diamond League participation, and gave no visible signal anywhere (console, CSV, or UI) that a discipline's predictions came from this weaker fallback instead of real standings. Not currently triggered for any discipline, but a real risk the user's exact question could resurface without warning if it ever does.
- **Fixed by making the distinction real and visible** instead of leaving it an unstated implementation detail: `run.py` now prints a `WARNING` when the fallback triggers for a discipline, and tags every prediction row with a real `dl_qualified` boolean reflecting which path produced it. `api.py`'s `"qualified": True` (previously a hardcoded no-op for every athlete — already flagged as a known minor issue before this session) now reads that real flag instead, defaulting to `True` for older CSVs written before the column existed (verified accurate for today's data specifically, not a guess). The frontend's "Q" badge — which already existed but meant nothing — now carries a real tooltip ("Confirmed in World Athletics' own 2026 Diamond League standings for this discipline") and reflects a real fact. **`predictions_latest.csv` itself won't visibly change from this** (today's data was already 100% standings-based) — the value is in the guarantee going forward, and honest labeling the moment the fallback ever does trigger.
- **Table spacing**: measured live via `getBoundingClientRect()` that every column in `DisciplineTable` had exactly 0px gap between adjacent `<td>`s — the "Athlete" column (no width constraint) was absorbing ~58% of the table's width (662px for names as short as "Noah LYLES"), while Projected (112px) and Win probability (208px) sat flush against each other and every other column with no breathing room at all, reading as one cramped cluster — matching the user's exact complaint. Fixed with real `pl-3`/`pl-4`/`pl-6`/`pl-8` left padding on each column (increasing progressively toward the denser numeric columns on the right) rather than resizing columns, plus widened `Win probability` slightly (`w-52`→`w-56`) and the table's `min-w` (`560px`→`680px`) to keep the added padding from squeezing content at the responsive floor. Verified via computed-style padding values live (not just visually) given this Browser pane's screenshot tool renders pages compressed/unreliable in this environment (documented repeatedly in earlier sessions).

**Real 2026 in-season "Season Form" chart, same session, immediate follow-up.** Having just confirmed Rai Benjamin genuinely raced 3 real DL meetings this season (via `WebFetch`/`WebSearch` against the live standings page and the official Diamond League rules page — 14 series meetings April-September, each discipline contested at only a subset of them, points-based top-6/8/10 qualification matching `get_qual_limit()` exactly, plus a real wildcard provision the app doesn't model), the user asked the natural next question: if that season history is real, why does the profile page's chart show a *prior* season instead of it?

- **Root cause, not a bug**: `data/raw/{discipline}.csv` (the file `load_athlete_history()` read) is deliberately capped at 2018-2025 by `season_results_scraper.py` (`YEARS = range(2018, 2026)`), because that script's real job is building *training* features, which need a completed season with a real Final result to label — extending it to 2026 would be wrong for that purpose. The tool that could fetch real 2026 per-meeting data was simply never pointed at the current season, because that wasn't its job.
- **Built `src/current_season_scraper.py`**, reusing `season_results_scraper.py`'s exact proven mechanism (`find_season_meetings`/`scrape_meeting`, same real WA GraphQL API, same "GW"-only regular-season filter, same recognized-athlete filter) but parameterized by year and writing to its own dedicated file, `data/raw/{discipline}_{year}_meetings.csv` — deliberately **not** merged into either `data/raw/{discipline}.csv` (would leak un-labelable current-season rows into training data) or `data/raw/{discipline}_2026.csv` (the live worldwide toplist snapshot `live_fetcher.py` fully overwrites every `run.py` run — writing there too would risk either script clobbering the other's data). Verified live before building anything: `find_season_meetings(2026)` returns 13 real completed meetings this season (close to the dashboard's own "12 of 15 meets done" stat), confirming the mechanism works before investing in the full build.
- **Ran it**: real per-meeting rows for all 32 disciplines, e.g. Rai Benjamin now shows 4 real 2026 races (44.11→44.13→44.05→43.83, a real improving trend) instead of falling back to an old season. Re-audited all 237 predictions-list athletes after: **232 now show real 2026 season form** (up from 0), 1 correctly still falls back to a prior season (an athlete who hasn't raced this particular discipline at a DL meeting yet this year — the fallback logic from the earlier per-athlete-season-year fix is still doing real work), 4 unchanged edge cases have no data anywhere.
- **`api.py`'s `load_athlete_history()` now checks `data/raw/{disc_key}_{MEETS_YEAR}_meetings.csv` first**, using real 2026 rows when present, falling back to the athlete's own most-recent-year-with-data historical logic (from the earlier session's fix) otherwise. Both paths share one dedup/formatting helper (`_season_rows_to_history()`) rather than duplicating the row-building logic twice.
- **One real bug caught during this**: the current-season scraper's output has no `Results Score` column at all (that field only ever came from the historical toplist scrape) — the shared dedup helper assumed it always existed and crashed with a `KeyError` on the very first live test. Fixed by defensively adding the column as empty when absent, caught by actually running the code against real data, not just reading it.
- **A display-only Unicode scare, not a real bug**: a venue name ("Meeting International d'Athlétisme Herculis EBS") showed up as "d�Athl�tisme" when printed in ad-hoc verification scripts in this Windows terminal — confirmed via raw bytes that the CSV, the live JSON API response, and the actual browser rendering are all byte-correct UTF-8 throughout; the mangling was purely this terminal's cp1252 console codepage failing to *display* the character, not real data corruption. Worth remembering: don't trust a terminal `print()` of scraped non-ASCII text as evidence of a real encoding bug without also checking raw bytes or the actual browser render.
- **Follow-on inconsistency, since fixed same session**: the profile page's "Meets this season" stat tile initially disagreed with the new chart's real race count for the same athlete (e.g. showed "1" while the chart showed 4 real races) — see the `meets_count` reconciliation entry further down for the real root cause and fix.
- Not part of this build: the real Diamond League wildcard provision (confirmed to exist via the official rules page) still isn't modeled anywhere in the pipeline — an athlete who qualifies for the real Final via wildcard rather than standings points wouldn't be reflected. Not attempted (wildcards aren't announced far in advance and aren't reliably scrapable ahead of the actual Final) — noted here in case it ever becomes relevant.

**Two small profile-page fixes + a real "days since last competed" bug, same session, immediate follow-up.** User asks: Age showed a decimal (e.g. "29.1", not meaningful at this precision); "Last raced" doesn't make sense for field events (they don't race); and — the substantial one — some athletes who'd clearly competed within the last day or two still showed "Last competed: 50d ago" or similar.

- Age now rounds to a whole number (`Math.round`), and the label reads "Last competed" instead of "Last raced" (`src/routes/athlete.$discKey.$name.tsx`).
- **The stale "last competed" was a real methodology bug, not just staleness** — confirmed live before fixing: Rai Benjamin's toplist-snapshot row (`data/raw/men_400m_2026.csv`, one row per athlete — their single season-best mark) was dated 18 Jul, because that happened to be his fastest time, even though he'd actually raced again on 23 Aug without beating it. `run.py`'s `build_2026_features()` computed `days_since_last` from that same one-row-per-athlete snapshot's date, which structurally can only ever mean "days since their BEST mark" — silently wrong for any athlete whose best mark wasn't also their most recent race, independent of how fresh the scrape is.
- **Fixed at the root**: `build_2026_features()` now prefers the real per-meeting current-season file (`current_season_scraper.py`'s output, one real row per real race) when it exists, computing `days_since_last`/`recent_trend` from an athlete's actual most recent competition instead of their best mark's date. Takes effect for predictions generated by any future `run.py` run.
- **Immediate fix for that day's already-generated data, without a full ~1hr `run.py` rescrape**: added `src/refresh_current_season_stats.py` (originally `refresh_days_since_last.py`, generalized — see below), a small, honest, narrowly-scoped patch script that recomputes just the `days_since_last` column in the existing `outputs/predictions_latest.csv` from the same real, already-scraped current-season meeting data — deliberately does **not** touch `win_probability` or any other model-fed value (days_since_last/recent_trend are also trained features, so a fully consistent refresh of the model's actual predictions still needs a real `run.py` run; this only corrects the one directly-displayed stat that was reported wrong). Ran it: 232 athletes' `days_since_last` corrected, e.g. Benjamin's flipped from 36 days to the real 1.
- **A near-miss worth remembering**: `run.py` has no `if __name__ == "__main__":` guard — it's a straight-through script that kicks off the full ~1hr live scrape the moment it's imported as a module. Importing it to unit-test the `build_2026_features()` fix in isolation accidentally triggered a real live scrape; caught and killed within about a minute via `TaskStop`, confirmed `outputs/predictions_latest.csv`'s mtime was untouched (that file is only written at the very end of the run, after all disciplines are processed) so no partial/corrupt state resulted. Verified the fix a different way instead — replicated just the changed calculation standalone against real data on disk. **Don't `import run` for testing purposes** — either run it as a subprocess/CLI, or extract logic into a function-only module first.

**Full `run.py` refresh, user-requested, same session.** Ran the real ~1hr live pipeline end to end (no errors) to make `win_probability` and every other model-fed field fully consistent with the corrected `days_since_last` logic above, not just the display-only patch. Confirmed live: Benjamin's `season_best` correctly updated to his real, faster 23 Aug mark (`43.83`, previously stuck on the stale 18 Jul `44.05`), which moved him from a stale rank #4/2% to a real rank #2/14% — the whole chain (scrape → features → model → display) is now consistent, not just the one patched field. `outputs/predictions_latest.csv`/`data/standings.json` were regenerated as part of this — see the git-tracking note in Known Limitations for why they don't show as new commits.

**`meets_count` reconciled with the real chart, same session, immediate follow-up.** User: the "Meets this season" stat tile still didn't match the chart's real race count (flagged as a known gap in the previous entry). Root cause was the exact same bug class as `days_since_last`, just more severe: `build_2026_features()`'s `meets = df.groupby("athlete_name")["Mark"].count()` counted rows in the toplist snapshot, which structurally has **exactly one row per athlete no matter what** — so `meets_count` wasn't just occasionally stale, it was *always* 1 for literally every athlete in the dataset, confirmed by inspection, not assumed. Fixed the same way: `build_2026_features()` now prefers a real per-athlete row count from the current-season meetings file when available, falling back to the toplist's structural 1 only for athletes with no current-season meeting data on file. `refresh_days_since_last.py` was generalized into `refresh_current_season_stats.py` (same script, now fixes both `days_since_last` and `meets_count` together) and re-run against the just-refreshed `predictions_latest.csv`: 226 athletes' `meets_count` corrected (e.g. Benjamin: 1 → 4, exactly matching his chart's 4 real races). Verified live via the actual API response and the rendered profile page, not just the CSV.

## Architecture / Key Files

- `run.py` — master pipeline: scrape live 2026 data → injury check → load trained model → build features → predict → `outputs/predictions_latest.csv`
- `api.py` — Flask bridge serving predictions + injury data to the dashboard (`/api/predictions`); also `/api/athlete/<discKey>/<name>` (`build_athlete_profile()`) for the profile page — real per-meet history, real head-to-head, real photo lookup (`load_athlete_photo()`, via WA's `getAthleteActionPictureByIds`)
- `src/train_model.py` — retraining entry point. `--with-recency --with-h2h` for the full feature set; `--dry-run` to backtest without overwriting `outputs/`; `--tune` to grid-search hyperparameters
- `src/dl_final_results_scraper.py` — real DL Final ground-truth labels (`data/dl_final_results.csv`), scraped from WA's GraphQL API
- `src/season_results_scraper.py` — real per-meeting season history, enriches `data/raw/{discipline}.csv` (2018-2025 only, feeds model training)
- `src/current_season_scraper.py` — real per-meeting results for the CURRENT in-progress season (`data/raw/{discipline}_{MEETS_YEAR}_meetings.csv`, separate file), powers the profile page's real 2026 "Season Form" chart; not part of `run.py`, run it separately/periodically
- `src/refresh_current_season_stats.py` — cheap, targeted fix for `outputs/predictions_latest.csv`'s `days_since_last`/`meets_count` columns between full `run.py` runs, using `current_season_scraper.py`'s already-scraped data; does not touch `win_probability` or other model-fed columns
- `src/major_meets_scraper.py` — real per-meeting results from Olympics/Worlds/Continental Tour Gold/Euro Champs, also enriches `data/raw/{discipline}.csv`
- `src/historical_scraper.py` — season-best toplists 2018-2025 (`data/raw/{discipline}.csv` base layer)
- `src/live_fetcher.py` — scrapes current-season (2026) standings/toplists for live predictions
- `src/injury_checker.py` — injury/withdrawal detection + severity estimation (`data/injury_flags.json`)
- `src/h2h_calculator.py` / `data/h2h/h2h_rates.csv` — head-to-head win rates, a trained feature
- `tests/` — unit tests, `python -m pytest`
- `src/components/dl/shell.tsx`, `src/lib/dl-data.ts` (track-insights-main) — dashboard shell (page wrapper + `Panel`/`RankBadge`/`ProbabilityBar`/`WatchBadge` primitives, ambient background layer) + API data contract
- `src/components/dl/topnav.tsx` (track-insights-main) — the top navigation bar (centered links), replaced `sidebar.tsx` (deleted)
- `src/routes/index.tsx` (track-insights-main) — the "/" landing page; `src/routes/dashboard.tsx` — the actual dashboard (has its own hero banner), moved here from "/"
- `src/components/dl/logo.tsx`, `PRODUCT.md` (track-insights-main) — PodiumCall's logo mark + durable product context for the `impeccable` skill
- `src/components/dl/track-circuit.tsx` (track-insights-main) — landing hero's animated running-track graphic; `src/components/dl/track-curve.tsx` — the static app-wide corner-curve decoration
- `src/hooks/useCountUp.ts` (track-insights-main) — rAF-based count-up animation used by Dashboard's stat tiles
- `src/routes/athlete.$discKey.$name.tsx` (track-insights-main) — the athlete profile page; `src/hooks/useAthleteProfile.ts` — its fetch hook
- `src/components/dl/season-trend-chart.tsx`, `head-to-head-chart.tsx`, `radial-meter.tsx` (track-insights-main) — the profile page's real charts (see dataviz skill for the design procedure behind them)
- `src/styles.css` (track-insights-main) — all theme tokens (`:root` for the app, `.landing` for the landing page's own composited tokens) plus shared utilities (`card-shadow`, `ambient-glow`, `ambient-grain`, `track-surface`, `stagger-in`)

## Failed Attempts (tried, verified, did not help — don't redo without new evidence)

- **Event-specific models** (separate RandomForest per event group). Scored identically to one pooled model — `season_rank`/`season_percentile` already normalize per-discipline, so separate models add nothing.
- **`h2h_meetings` (count) as an additional feature** alongside `h2h_win_rate`: made results slightly worse. Not included.
- **Manual h2h blend at fixed weights**: tied with the trained-feature approach at best. Trained-feature chosen for architectural consistency (a manual post-hoc blend was the exact pattern that caused the recency-feature bug — didn't want to reintroduce it for h2h).
- **`src/scraper.py` (Wikipedia-based)**: fully non-functional, 0 rows ever. Deleted.
- **Including the DL Final's own results in `season_results_scraper.py`** (`rankingCategory in {"GW","DF"}`): produced a suspiciously large accuracy jump (63.8%) that turned out to be label leakage — the Final's own result was leaking into that year's training features. Fixed by restricting to `"GW"` only. **Lesson: a suspiciously good number is a signal to check harder, not a result to bank.**
- **Real per-athlete race-log scraping (all meets worldwide, not just the DL circuit)** (2026-08-23): confirmed via GraphQL introspection that WA's public API schema *has* fields for this (`getSingleCompetitorResultsDiscipline`/`getSingleCompetitor.resultsByDate`, taking a competitor id from `searchCompetitors`) — but the resolvers are dead server-side. Every combination of athlete id, year, and order-by argument tried (including with browser-matching headers) returned `resultsByDate: null`, while sibling fields on the same object (`activeYears`) resolved fine. `getSingleCompetitorSeasonBests` *does* work and confirms athletes' bests across all competitions (not just DL), but it's still one row per discipline per season — no more multi-meet granularity than what `season_results_scraper.py` already provides for the DL circuit specifically. **Don't re-attempt this without new evidence the resolver was fixed** — it's a dead field, not a query-construction mistake.
- **Widening `major_meets_scraper.py`'s Area Championships from European-only to all six continents** (African/Asian/South American/Oceania/NACAC added, 2026-08-23, sixth session): a live probe confirmed the calendar group (id 3660) is cleanly scoped (all six tagged `rankingCategory: "GL"`, no junior/indoor contamination) — the *code* concern that blocked this originally didn't hold up. But the *data* did: re-scraping (+150-390 rows/discipline) and retraining dropped accuracy 60.1% -> 58.6%, and re-tuning hyperparameters on the new data only recovered to 58.8% (best of 54 candidates), still below baseline. **This is a real regression, not tuning noise** — reverted via `git checkout -- src/major_meets_scraper.py` plus a re-scrape/re-train to restore the exact 60.1% baseline (confirmed byte-identical `outputs/*.pkl` after, since RF is deterministic with `random_state=42` on identical input). Likely cause: five of the six continental championships have much more field-quality variance than DL/Worlds/Olympics/Euro Champs, and the recognized-athlete filter doesn't fully compensate. **Don't redo without a per-continent accuracy breakdown first** (e.g. try adding just one continent at a time) if this gets revisited.
- **Three new "outcome, not just speed" feature candidates** (2026-08-23, sixth session, same day as the above): brainstormed features not derivable from the current 14 (all of which measure "how fast/far", not "did they actually win"), built and tested at fixed hyperparameters (isolated from tuning noise, same discipline as the Area Championships test above):
  - `final_top3_rate` — fraction of the season's *actual final-round* races (parsed from the raw toplist's `Pos` column, e.g. `"1"`/`"2f1"` = final, `"1h1"`/`"1sf1"` = heat/semi and excluded) where the athlete placed top 3. **58.8% alone** (-1.3pts). First hypothesis: `Pos` only existed in the original toplist-scrape rows (~55% of a discipline's rows), so ~45% defaulted to 0.0 for structural (source-confounded) reasons, not because the athlete never placed well. **Tested and falsified**: `season_results_scraper.py`/`major_meets_scraper.py` turned out to already fetch WA's `place` field per result and just never write it to the row (see the standalone fix, commit `c391a719`) — a one-line, zero-new-scraping-cost bug, not a missing capability. Backfilling it raised `Pos` coverage to ~99%, but accuracy came out **58.4%, still below baseline** (and no better than the original 58.8%). Real cause, confirmed by inspection: this data slice (DL circuit + major meets only, not an athlete's full season) gives most athletes just **1** final-round race in a given season — `final_top3_rate` is then a coin-flip point estimate from a single race, not a real behavioral tendency, and its raw correlation with the actual `dl_top3` label is 0.058 (essentially none). **The `Pos`-coverage fix was kept anyway** (correct, free, more complete data on disk) but the feature itself was not — don't re-attempt without a way to get more than ~1 final-round observation per athlete per season first (i.e. this is a data-density problem, not a coverage or engineering problem).
  - `dl_meets_share` — fraction of the season's marks that came from actual Diamond League venues (reusing the same `DL_VENUES` list `competition_weight` already checks). **59.3% alone** (-0.8pts). Root cause hypothesis: likely redundant with `weighted_season_best` (which already upweights DL-venue marks) rather than adding new information.
  - `dl_final_experience` — count of prior years (strictly before the label year, no leakage) this athlete made a DL Final in this discipline at all, from the full field in `dl_final_results.csv` (not just podiums). **58.2% alone** (-1.9pts). Root cause hypothesis: caps out low (dataset only spans 2018-2025) and likely correlates with `career_best`/`age` already, adding noise rather than signal.
  - **All three together: 57.1%** (-3.0pts) — worse than any single one, consistent with each contributing real noise rather than the drop being one bad feature diluted by two good ones. **Every one of the three regressed individually at the exact hyperparameters that produce the clean 60.1% baseline** — this isn't tuning noise, it's a real (if modest per-feature) negative signal. Code was reverted entirely (`git checkout -- src/train_model.py`, nothing had been committed) rather than left as an unused opt-in flag, matching how `h2h_meetings`/the manual h2h blend were handled above. (`final_top3_rate`'s `Pos`-coverage gap noted here was fixed and retested — see the next entry — and it still didn't help; the note above is superseded.)
- **Worldwide-style data expansion prototype** (2026-08-23, sixth session, same day): rather than commit to a full worldwide scrape untested, prototyped the cheapest reachable version first — World Athletics Continental Tour (id 3773) already has all-tier data (Gold/Silver/Bronze/Challenger) sitting behind the existing `major_meets_scraper.py` query, currently filtered to Gold only. Widening to all tiers, scoped to 3 disciplines (`men_100m`, `women_800m`, `men_LJ`) and 3 recent years (2023-2025, not the full 2018-2025 range) to keep it cheap: **~275+ meetings/year in this one competition group alone** (confirmed via a month-by-month count after the naive full-year query silently capped at exactly 100). Added +1187 to +1334 recognized-athlete rows per discipline even after the existing noise filter dropped ~2000+ unrecognized rows per discipline (this really is much noisier data, as originally suspected).
  - **This did fix the specific density problem `final_top3_rate` died on**: median final-round races per athlete-season went from 1.0 to 3.0-4.0 across all three disciplines. A from-scratch `final_top3_rate`-style correlation check on just this enriched slice improved in all three (men_100m 0.050→0.074, women_800m 0.054→0.105, men_LJ -0.031→0.038) — the first time in this session's whole accuracy investigation that "add more data" moved a signal in the right direction.
  - **But the real backtest, using the current shipped feature set (not a new feature), got worse**: 48.1% → 37.0% on just these 3 disciplines' 2023-2025 folds (13/27 → 10/27 correct). The lower-tier marks flow straight into `season_best`/`consistency`/`weighted_season_best`/`meets_count` for these athletes, and apparently dilute those aggregates with noisier, less-competitive-intensity marks faster than the added density helps. **Caveat: N=27 is small enough that this specific 3-hit swing carries real sampling noise of its own** — but it's consistent with, not contradicting, every other finding this session.
  - **Verdict: does not justify the full 32-discipline/7-year rollout.** The one encouraging signal (isolated correlation for a hypothetical feature) didn't survive contact with the real target metric once mixed into the existing aggregate features. Prototype data was stripped back out of `data/raw/{men_100m,women_800m,men_LJ}.csv` (filtered by its `ct_full_proto` source tag) to restore the shipped state — nothing was committed (data/raw/ is gitignored). **If this gets revisited, the fix would need to be about *how* lower-tier marks are weighted/blended into the aggregate features (e.g. a real competition-tier discount, not `competition_weight`'s current binary DL/major-vs-everything-else split), not just adding more of them at equal weight** — don't just re-run the same prototype expecting a different result.

## Known Limitations (real, understood, not bugs to re-chase)

- `season_results_scraper.py` (and `current_season_scraper.py`, same underlying mechanism, eighth session) only covers the Diamond League circuit, not an athlete's entire season everywhere they raced. An athlete who mostly raced non-DL meets still shows thinner history than reality. Real per-athlete profile scraping would close this fully but WA's API doesn't currently expose it (see Failed Attempts above) — the only way to close this now would be scraping every meeting worldwide via the general calendar, a much bigger undertaking with real rate-limit/politeness concerns at that scale.
- `men_5000m` has real labels for only 1 of 5 years (2024) — other years ran a 5km road race or didn't contest it in a form the scraper recognized. Auto-detected from real data, not hand-flagged, but the practical effect is thin signal for this one discipline.
- 2020 alone is excluded from training labels (COVID-era "Inspiration Games" exhibition, not a real qualifying Final). 2018/2019 are now included — see the Ground Truth note above for why they turned out not to need special handling after all.
- Two specific qualified athletes (Agnes Jebet Ngetich, women's 5000m; Yemisi Mabry, women's shot put) are genuinely absent from World Athletics toplists even at top-500 depth. Not fixable via more pagination.
- Women's Shot Put / Women's 5000m predictions are consistently short by 1-2 athletes.
- Projections page's per-meet trajectory chart is fabricated interpolation, honestly labeled but not rebuilt — deliberately deprioritized by the user.
- ~37% of athletes (86/235, seventh session's live audit) genuinely have no action photo on World Athletics' own system — `load_athlete_photo()` correctly returns `None` and the frontend falls back to an initials monogram. Not fixable from this endpoint; see the profile-page note above for the one real bug that *was* fixed here (a URL-format regex gap) and why it didn't move the number.
- The profile hero photo's crop position now comes from real per-photo face detection (`api.py`'s `detect_face_focus()`, OpenCV DNN, seventh session) with a `center 15%` fallback only when no confident face is found (~6 of the ~32 sampled athletes with a real photo, mostly side-profile or mid-air shots — verified live, see the profile-page note above for the full list). An unusual photo can still fall back to the imperfect default in that case; there's no further fix planned unless it turns out to matter often enough in practice.
- `outputs/predictions_latest.csv` and `data/standings.json` were both listed in `.gitignore` (correctly — they're regenerable, `run.py`/`live_fetcher.py`-owned outputs) but stayed **tracked** in git from an old commit (`ab909aa6`), predating whenever the gitignore rule was added. Noticed 2026-08-24, fixed the same day at the user's request via `git rm --cached` on both (commit `dac3adf1`) — files themselves untouched on disk, `git status` no longer shows them as dirty after a `run.py` run.
- Minor, not acted on: `lovable-error-reporting.ts` does nothing outside the Lovable editor; React Query is wired in but `usePredictions.ts` still does manual `fetch`/`useState`. (The `"qualified"` field used to be a hardcoded no-op — fixed eighth session, see above, it's real now.)

## Next Steps

1. **Athlete profile page is shipped, including the table-view toggle, Projections-page links, the photo-lookup regex fix, real per-photo face detection, the real "Q" qualified badge, a real current-season "Season Form" chart, and real `days_since_last`/`meets_count` (all eighth session) — no further ask from the user yet, so don't assume more work is wanted here without being told.** If it does come up again, real candidates worth knowing about: (a) `run.py`'s exported stats (`career_best`/`pb_gap`/`age`) are currently only used by the profile page — could also enrich the Track/Field discipline tables if that's ever wanted; (b) the Dashboard's "Removed from predictions" list still doesn't link out (see the profile-page note above for why — those athletes aren't in `predictions_latest.csv`, so there's no profile to link to without a separate data source); (c) the face detector's non-face fallback (`center 15%`) is still a heuristic for the minority of photos with no confident detection — not worth touching unless it turns out to matter in practice; (d) `current_season_scraper.py`/`refresh_current_season_stats.py` aren't wired into `run.py`'s own refresh cycle, need to be run separately/periodically as new DL meetings happen.
2. **Landing page "a little more work" ask has had one polish round** (sixth session, same day, pushed as `a8f0d7b`): craft-floor visual fixes via the impeccable skill (kicker removal, card depth, ::selection/scrollbar theming, ticker a11y) plus one purposeful scroll-triggered motion moment via the animate skill on the demo section. The user tried and backed out of a Figma-based motion workflow first (no file was ever shared) before asking for the code-based version instead — no Figma file is in the loop for this project. If the user wants further landing-page work, ask what specifically rather than guessing a new direction (guessing wrong on visual direction cost real rework multiple times in the fifth session — see the "working methods" notes above).
3. **Accuracy work has now been thoroughly, honestly exhausted for this session** — five real attempts beyond the original 2018/2019-labels + noise-filter wins, all reverted: all-six Area Championships (data-quality dilution), three new feature types (mostly a sample-size problem, one root-caused via a real scraper bug fix that was kept), and a worldwide-style data expansion prototype (Continental Tour all tiers, 3 disciplines, 3 years — fixed the diagnosed density problem and improved isolated correlation, but made the *real* backtest worse by diluting existing aggregate features with noisier lower-tier marks). The headline number has sat at 60.1% through all of it. **This is no longer "an unlucky run" — it's well-investigated evidence of a real ceiling for this dataset/feature-set combination.** If accuracy work resumes: the worldwide-scraping idea isn't fully dead, but the fix would need to be about *properly weighting* lower-tier competition (not just adding more of it at equal weight — see that Failed Attempt entry) — don't just re-run the same prototype at bigger scope expecting a different result. Otherwise, recommend explicitly asking the user before spending more time chasing accuracy candidates; the deprioritized polish items or routine maintenance are the better use of time until a genuinely new idea (not a variant of something already tried) comes up.
4. **Test coverage**: done for the Python pipeline — `build_labeled_dataset()`/`train_and_backtest()`/`load_predictions()` are covered end to end via `tests/fixtures/` + `tests/test_fixtures_integration.py`. If further expanding: `run.py`'s `build_2026_features()`, the injury-checker's scraping logic (both need Selenium/live network, harder to fixture), and the new `build_athlete_profile()`/`load_athlete_photo()` in `api.py` are all still untested — the frontend has no test suite at all yet (typecheck/lint only).
5. **Other deprioritized polish, still not started**: READMEs for both repos, a real per-meet Projections chart (still fabricated interpolation, see Known Limitations), React Query refactor (`usePredictions.ts` still does manual `fetch`/`useState`), and the injury Watch badge's underlying extraction quality (`injury_checker.py`, Python side — currently surfaces raw scraped headlines, not structured injury descriptions; the frontend now presents this honestly but the extraction itself hasn't been improved).
6. **Otherwise**: the system is in good shape 12 days out from the Final. Rerun `run.py` after Zurich (Aug 27) and again closer to Sep 4-5 to pick up final-season data — nothing else is currently broken or blocking.
